---
phase: 16-playbook-migration-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.local
  - .env.example
autonomous: true
requirements: [DE1.1, DE5.1, DE5.4]
user_setup: []

must_haves:
  truths:
    - "API_KEY in .env.local is ≥32 characters"
    - "API_KEY in .env.example is ≥32 characters"
    - "Both files have identical length API_KEY (same value for dev consistency)"
    - ".env.example documentation explains minimum 32-char requirement"
    - "API key format preserves sk_test_ prefix for dev keys"
  artifacts:
    - path: ".env.local"
      provides: "Development API key with minimum 32 chars"
      must_contain: ["API_KEY=sk_test_"]
      min_length_value: 32
    - path: ".env.example"
      provides: "Template with documented API key requirement"
      must_contain: ["API_KEY=", "32+", "characters"]
  key_links:
    - from: ".env.local"
      to: ".env.example"
      via: "both use same dev key value"
      pattern: "API_KEY=sk_test_"
    - from: "ENV config"
      to: "API validation"
      via: "Express middleware checks API_KEY header length"
      pattern: "X-API-Key.*32.*chars"
---

<objective>
Fix API_KEY length to meet minimum 32-character requirement in both development and template environments. Ensures API authentication works correctly and matches security best practices.

Purpose: API key validation in Express middleware requires ≥32 chars. Current dev key "dev-api-key-change-in-production" is only 31 chars and fails validation. Template documentation must clearly state minimum requirement.

Output:
- .env.local with new sk_test_-prefixed API key ≥32 chars
- .env.example updated with same key and enhanced documentation
- Both files use identical dev key for consistency
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Current state:
- .env.local (line 12): API_KEY="dev-api-key-change-in-production" (31 chars, too short)
- .env.example (line 28): API_KEY=sk_test_your_secret_key_here_min_32_chars (66 chars, meets requirement but placeholder)

Key requirement from .env.example lines 105-110:
"API_KEY: Generate using: openssl rand -hex 32"
"Keep this secret! Use strong random strings (32+ characters)"
"API KEY SECURITY: Generate using: openssl rand -hex 32"
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate New 32-Character API Key for Development</name>
  <files>.env.local</files>
  <action>
Generate a new API key with minimum 32 characters using the command-line approach documented in .env.example.

Run: `openssl rand -hex 16`
This generates 32 hex characters (16 bytes * 2 hex chars per byte = 32 total characters).

Example output: `abc1234567890def1234567890abcdef` (32 chars)

Then format as: `sk_test_abc1234567890def1234567890abcdef` (46 total: 8-char prefix + 32-char random)

Action steps:
1. Generate the random hex string: `openssl rand -hex 16` 
2. Capture the output (32-character hex string)
3. Prepend `sk_test_` prefix to create full API key
4. Update .env.local line 12 from:
   `API_KEY="dev-api-key-change-in-production"`
   to:
   `API_KEY="sk_test_{generated_32_char_hex}"`
5. Verify the new key is exactly 8 + 32 = 40 characters total

Example (DO NOT copy this exact value — generate a new one):
   `API_KEY="sk_test_3a2b1c9d8e7f6a5b4c3d2e1f0a9b8c7d"`

Rationale:
- sk_test_ prefix identifies this as a test/development key (not production sk_live_)
- 32+ character random hex ensures strong entropy
- Matches the format and length required by API middleware validation
- Documented in .env.example as the standard approach
  </action>
  <verify>
    <automated>grep "^API_KEY=" /c/Users/PascalAmmeter/ClaudeCode/siag-incident-assistant/.env.local | sed 's/API_KEY="//' | sed 's/"//' | wc -c</automated>
  </verify>
  <done>
.env.local updated. API_KEY line now has a sk_test_-prefixed key with exactly 40 characters (8-char prefix + 32-char hex random). Verified: new key ≥32 characters, follows format convention.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update .env.example with Same Dev Key and Enhanced Documentation</name>
  <files>.env.example</files>
  <action>
Update .env.example to use the SAME API key value as .env.local (for consistency during development) and ensure documentation clearly states the 32+ character requirement.

Current state:
- Line 28: `API_KEY=sk_test_your_secret_key_here_min_32_chars` (placeholder)
- Lines 23-27: Documentation about API key format
- Lines 105-110: Notes section with openssl generation command

Action steps:
1. Replace placeholder on line 28 with the NEW API key from Task 1:
   OLD: `API_KEY=sk_test_your_secret_key_here_min_32_chars`
   NEW: `API_KEY=sk_test_{same_key_as_env_local}`
   
   This ensures developers can immediately copy .env.example → .env.local without modification for local dev.

2. Verify documentation at lines 23-27 is clear:
   ```
   # API Key for authentication (X-API-Key header)
   # Development keys should start with: sk_test_
   # Production keys should start with: sk_live_
   # Keep this secret! Use strong random strings (32+ characters)
   # Generate: openssl rand -hex 16 | tr -d '\n' && echo
   ```
   
   This is correct, but enhance line 27 command for clarity. Update to:
   ```
   # Generate with: openssl rand -hex 16
   ```
   (Remove the pipe to tr/echo — just show the core command)

3. Verify Notes section (lines 85-90) includes the setup instruction:
   ```
   # 1. DEVELOPMENT SETUP:
   #    - Copy this file to .env.local
   #    - Fill in DATABASE_URL and DIRECT_URL from Neon
   #    - Generate a new API_KEY using: openssl rand -hex 16
   #    - Set CORS_ORIGIN=http://localhost:3000
   #    - Run: npm run dev:backend
   ```
   
   This is already correct and clearly documents the generation method.

4. Verify section "4. API KEY SECURITY:" (lines 105-110) is present and correct:
   ```
   # 4. API KEY SECURITY:
   #    - Generate using: openssl rand -hex 32
   #    - Store only in environment variables (never in code)
   #    - Rotate keys every 90 days (v1.2+)
   #    - Use different keys for dev/staging/production
   #    - Revoke compromised keys immediately
   ```
   
   NOTE: This command says `openssl rand -hex 32` but should be `openssl rand -hex 16` (16 bytes → 32 hex chars). Fix this discrepancy.
   Update line 106 from:
   OLD: `# 4. API KEY SECURITY:\n#    - Generate using: openssl rand -hex 32`
   NEW: `# 4. API KEY SECURITY:\n#    - Generate using: openssl rand -hex 16`

Summary of changes:
- Line 28: Replace placeholder with actual sk_test_ key (32+ chars)
- Line 27: Simplify generation command (remove tr/echo)
- Line 106: Fix openssl command (hex 16, not 32)
- Everything else: Keep as-is (documentation is already good)
  </action>
  <verify>
    <automated>grep -A 5 "^API_KEY=" /c/Users/PascalAmmeter/ClaudeCode/siag-incident-assistant/.env.example | head -1 | wc -c</automated>
  </verify>
  <done>
.env.example updated. API_KEY line (28) now has the same sk_test_ key as .env.local. Documentation clarified at lines 27 and 106. Generation command corrected to `openssl rand -hex 16`. Verified: new key ≥32 characters.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify API_KEY in Both Files Matches and Meets Requirements</name>
  <files>.env.local, .env.example</files>
  <action>
Verify that:
1. Both .env.local and .env.example have the SAME API_KEY value
2. The API_KEY is exactly sk_test_-prefixed with 32+ character hex string
3. Total length is ≥40 characters (8 prefix + 32 min random)

Run verification checks:
```bash
# Extract API_KEY from both files
LOCAL_KEY=$(grep "^API_KEY=" .env.local | sed 's/API_KEY="//' | sed 's/"//')
EXAMPLE_KEY=$(grep "^API_KEY=" .env.example | sed 's/API_KEY=//')

# Check they match
if [ "$LOCAL_KEY" = "$EXAMPLE_KEY" ]; then
  echo "✓ Keys match"
else
  echo "✗ Keys differ!"
fi

# Check length
echo "Local key length: ${#LOCAL_KEY}"
echo "Example key length: ${#EXAMPLE_KEY}"

# Check format
if [[ "$LOCAL_KEY" =~ ^sk_test_ ]]; then
  echo "✓ Local key has sk_test_ prefix"
else
  echo "✗ Local key missing sk_test_ prefix"
fi

if [[ "$EXAMPLE_KEY" =~ ^sk_test_ ]]; then
  echo "✓ Example key has sk_test_ prefix"
else
  echo "✗ Example key missing sk_test_ prefix"
fi
```

Expected output:
```
✓ Keys match
Local key length: 40
Example key length: 40
✓ Local key has sk_test_ prefix
✓ Example key has sk_test_ prefix
```
  </action>
  <verify>
    <automated>bash -c 'LOCAL_KEY=$(grep "^API_KEY=" /c/Users/PascalAmmeter/ClaudeCode/siag-incident-assistant/.env.local | cut -d"\"" -f2); EXAMPLE_KEY=$(grep "^API_KEY=" /c/Users/PascalAmmeter/ClaudeCode/siag-incident-assistant/.env.example | cut -d"=" -f2); if [ "$LOCAL_KEY" = "$EXAMPLE_KEY" ] && [ ${#LOCAL_KEY} -ge 40 ] && [[ "$LOCAL_KEY" == sk_test_* ]]; then echo "PASS: Keys match and meet requirements"; else echo "FAIL: Key mismatch or requirement not met"; fi'</automated>
  </verify>
  <done>
Verification complete. Both .env.local and .env.example have identical API_KEY values. Key format: sk_test_{32_char_hex}. Total length: 40 characters (≥32 chars requirement met). All checks pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Environment Variable Input | API_KEY loaded from .env into process.env, used for request authentication |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Cryptography | Weak API Key | mitigate | Enforce minimum 32-character length; use sk_test_/sk_live_ prefixes; document rotation policy; never commit actual keys (template only) |
| T-16-04 | Information Disclosure | Key in Example File | accept | .env.example contains placeholder dev key only (clearly marked "your_secret_key_here"); never contains production keys; git includes .env.example but .env.local in .gitignore |
</threat_model>

<verification>

## Phase 16-02 Verification Checklist

- [ ] .env.local API_KEY updated from "dev-api-key-change-in-production" (31 chars)
- [ ] .env.local API_KEY is now sk_test_ prefixed with 32+ char hex (40 total)
- [ ] .env.example API_KEY updated to match .env.local value
- [ ] .env.example API_KEY is sk_test_ prefixed with 32+ char hex
- [ ] Both files have identical API_KEY values (verified via grep)
- [ ] .env.example line 27: Generation command is `openssl rand -hex 16`
- [ ] .env.example line 106: Security section corrected to `openssl rand -hex 16`
- [ ] All documentation points to same generation method
- [ ] No production keys or secrets in either file (sk_test_ = dev only)

## Success Criteria Met

✅ API_KEY in .env.local ≥32 chars
✅ API_KEY in .env.example ≥32 chars
✅ Both files have identical dev key for consistency
✅ sk_test_ prefix identifies keys as development/test
✅ Documentation explains 32+ character requirement
✅ Generation method documented consistently

</verification>

<success_criteria>

1. **API_KEY Length**: Both .env.local and .env.example have API_KEY ≥32 characters
2. **Format**: API_KEY uses sk_test_ prefix (development) or sk_live_ (production) convention
3. **Consistency**: .env.local and .env.example use identical dev key for local development
4. **Documentation**: Comments in .env.example clearly state minimum 32-character requirement and generation method
5. **Verification**: `grep "^API_KEY=" .env.{local,example}` shows both keys are ≥40 chars total

</success_criteria>

<output>
After completion, create `.planning/phases/16-playbook-migration-cleanup/16-02-SUMMARY.md` with:
- Execution time (actual)
- New API_KEY value (first 20 chars only, last chars masked for security) and length verification
- Files modified (.env.local and .env.example)
- Git commits created
- Any deviations from plan
- Verification commands run and results
</output>
