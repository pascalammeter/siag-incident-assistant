---
phase: 15-pdf-export-app-router
plan: 02
type: execute
wave: 1
depends_on: [15-01]
files_modified:
  - src/components/incidents/IncidentList.tsx
  - src/components/incidents/IncidentTable.tsx
autonomous: true
requirements:
  - B5.1
  - B5.2
  - B5.3
  - B5.4
must_haves:
  truths:
    - "IncidentList.handleExportClick calls GET /api/incidents/:id/export/pdf"
    - "IncidentTable export button is no longer a stub (console.log removed)"
    - "Export button shows loading state while PDF is being generated"
    - "PDF downloads to user's browser when response arrives"
    - "Export button is disabled while request is in flight (prevents double-submit)"
  artifacts:
    - path: "src/components/incidents/IncidentList.tsx"
      provides: "Container component with handleExportClick() implementation"
      exports: ["handleExportClick"]
    - path: "src/components/incidents/IncidentTable.tsx"
      provides: "Table row component with export button and loading state"
      exports: ["Export button JSX"]
  key_links:
    - from: "src/components/incidents/IncidentList.tsx"
      to: "GET /api/incidents/:id/export/pdf"
      via: "fetch() in handleExportClick with API key header"
      pattern: "fetch.*export/pdf"
    - from: "src/components/incidents/IncidentTable.tsx"
      to: "src/components/incidents/IncidentList.tsx"
      via: "onExportClick callback prop"
      pattern: "onExportClick"
---

<objective>
Wire the IncidentList export button to call the new PDF export API route and trigger browser download.

**Purpose:** Replace the console.log stub in IncidentList.handleExportClick() with a real API call that generates and downloads PDF files.

**Output:** Updated `src/components/incidents/IncidentList.tsx` and `src/components/incidents/IncidentTable.tsx` that:
- Call `GET /api/incidents/:id/export/pdf` when export button is clicked
- Show loading spinner while PDF is generating
- Disable export button during request (prevent double-submit)
- Trigger browser download when PDF arrives
- Show error toast if export fails
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-pdf-export-app-router/15-CONTEXT.md
@src/components/incidents/IncidentList.tsx
@src/components/incidents/IncidentTable.tsx
@.planning/phases/15-pdf-export-app-router/15-01-SUMMARY.md

# From Phase 13 (Deployment) — how API calls are authenticated in the client
@src/hooks/useIncident.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Understand Current IncidentList & IncidentTable Structure</name>
  <files>src/components/incidents/IncidentList.tsx, src/components/incidents/IncidentTable.tsx</files>
  <action>
    Read and understand the current component structure:
    
    1. **IncidentList.tsx (container):**
       - Line 97-100: `handleExportClick(id)` is a stub that logs "Export not yet implemented"
       - Line 164: Passes `onExportClick={handleExportClick}` to IncidentTable
       - Component uses `useIncident()` hook for API calls (listIncidents, deleteIncident, etc.)
       - Check: How does useIncident() handle API key? Does it add X-API-Key header automatically?
    
    2. **IncidentTable.tsx (presentational):**
       - Receive `onExportClick` as a prop
       - Export button is in the table row — trigger onClick={onExportClick(id)}
       - Identify the export button element (look for text like "Export" or icon)
       - Note: Table may have loading states for other actions — reuse the pattern for export
    
    3. **API authentication:**
       - Review useIncident() hook to understand how it manages API key
       - Check if API key is stored in localStorage, context, or environment
       - Determine how to pass X-API-Key header in fetch() call
    
    4. **Error handling pattern:**
       - Review how deleteIncident() and other useIncident methods handle errors
       - Look for error toast pattern (showErrorToast usage)
       - Check if there's a success toast for successful operations
  </action>
  <verify>
    - Confirm handleExportClick exists at line ~97 in IncidentList.tsx
    - Confirm IncidentTable receives onExportClick callback
    - Confirm useIncident hook is imported and used in IncidentList
    - Confirm error handling pattern (try/catch, showErrorToast) is used elsewhere in component
  </verify>
  <done>Component structure understood — executor knows how to integrate fetch() call with API key and error handling</done>
</task>

<task type="auto">
  <name>Task 2: Implement handleExportClick in IncidentList</name>
  <files>src/components/incidents/IncidentList.tsx</files>
  <action>
    Replace the stub handleExportClick() with a real implementation.
    
    Implementation steps:
    
    1. **Add local loading state:**
       - Add `const [exportingId, setExportingId] = useState<string | null>(null)` near line 35 with other state
       - Use this to track which incident is currently exporting (used to disable button in IncidentTable)
    
    2. **Replace handleExportClick stub (lines 97-100):**
       ```typescript
       const handleExportClick = async (id: string) => {
         try {
           setExportingId(id);
           
           // Get API key from localStorage (same pattern as useIncident hook)
           const apiKey = localStorage.getItem('api_key') || process.env.NEXT_PUBLIC_API_KEY || '';
           
           // Call PDF export API endpoint
           const response = await fetch(`/api/incidents/${id}/export/pdf`, {
             method: 'GET',
             headers: {
               'X-API-Key': apiKey,
             },
           });
           
           if (!response.ok) {
             const errorData = await response.json();
             throw new Error(errorData.error || `HTTP ${response.status}`);
           }
           
           // Get PDF blob
           const blob = await response.blob();
           
           // Create download link and trigger download
           const url = window.URL.createObjectURL(blob);
           const link = document.createElement('a');
           link.href = url;
           link.download = `incident-${id}.pdf`;
           document.body.appendChild(link);
           link.click();
           document.body.removeChild(link);
           window.URL.revokeObjectURL(url);
           
           showSuccessToast('PDF erfolgreich exportiert');
         } catch (err) {
           const errorMsg = err instanceof Error ? err.message : 'Failed to export PDF';
           showErrorToast(`Export fehlgeschlagen: ${errorMsg}`);
         } finally {
           setExportingId(null);
         }
       };
       ```
    
    3. **Pass exportingId to IncidentTable:**
       - Add prop to IncidentTable: `isExporting={exportingId === incident.id}`
       - IncidentTable receives this and disables/shows loading on the matching row's export button
    
    4. **Import requirements:**
       - Ensure showSuccessToast and showErrorToast are imported (should already be available)
       - Verify useState is imported from 'react'
    
    5. **API key retrieval gotcha:**
       - Determine the exact localStorage key used (check useIncident hook or .env files for NEXT_PUBLIC_API_KEY)
       - If uncertain, review Phase 13 deployment plan or existing fetch calls in codebase
       - For now, use a reasonable default: `localStorage.getItem('api_key') || process.env.NEXT_PUBLIC_API_KEY || ''`
  </action>
  <verify>
    - Confirm `const [exportingId, setExportingId] = useState<string | null>(null)` is added to local state
    - Confirm handleExportClick is no longer a console.log stub
    - Confirm fetch() call targets `/api/incidents/${id}/export/pdf` with GET method
    - Confirm X-API-Key header is included in fetch options
    - Confirm response.blob() is used to get binary data
    - Confirm download link is created and programmatically clicked
    - Confirm error handling with try/catch/finally
  </verify>
  <done>IncidentList.handleExportClick() now calls API and triggers download</done>
</task>

<task type="auto">
  <name>Task 3: Update IncidentTable Export Button UI</name>
  <files>src/components/incidents/IncidentTable.tsx</files>
  <action>
    Update the IncidentTable component to show loading state and disable button during export.
    
    Implementation steps:
    
    1. **Update component signature:**
       - Add new prop to IncidentTable: `isExporting?: boolean` or `isExportingId?: string | null`
       - Recommend using `isExportingId?: string | null` and `currentIncidentId: string` to match exact incident
    
    2. **Find the export button:**
       - Search for the export button element in IncidentTable.tsx
       - It may be in a table row action column
       - Should have onClick={onExportClick(id)}
    
    3. **Update button element:**
       - Add conditional disabled state: `disabled={isExportingId === id}`
       - Add conditional loading spinner: `{isExportingId === id ? <LoadingSpinner size="sm" /> : 'Export'}`
       - Add conditional opacity: `opacity-50 cursor-not-allowed` when disabled
    
    4. **Alternatively (if button is shared):**
       - If export button is a reusable action button, add loading state prop
       - Pass loading state as prop: `<Button loading={isExportingId === id} onClick={() => onExportClick(id)}>Export</Button>`
    
    5. **Import LoadingSpinner if needed:**
       - If not already imported, add: `import { LoadingSpinner } from '@/components/atoms/LoadingSpinner'`
       - Or use a simpler inline spinner: `<span className="inline-block w-4 h-4 border-2 border-white/30 border-t-white rounded-full animate-spin" />`
    
    6. **CSS classes:**
       - Use Tailwind classes already in codebase for disabled state
       - Ensure button opacity matches other disabled buttons (e.g., 50% opacity in save button from Step6)
  </action>
  <verify>
    - Confirm IncidentTable receives isExporting prop from IncidentList
    - Confirm export button has disabled={isExporting} attribute
    - Confirm export button shows loading spinner when disabled
    - Confirm onClick callback still triggers handleExportClick
  </verify>
  <done>IncidentTable export button shows loading state and is disabled during PDF generation</done>
</task>

<task type="auto">
  <name>Task 4: Test Local Export Flow (Manual)</name>
  <files>src/components/incidents/IncidentList.tsx, src/components/incidents/IncidentTable.tsx</files>
  <action>
    Manually test the complete export flow:
    
    1. **Setup:**
       - Ensure Plan 15-01 is implemented (API route exists at /api/incidents/[id]/export/pdf)
       - Ensure at least one incident exists in the database (from a prior wizard save)
       - Get the incident ID from the incident list UI or database query
    
    2. **Test procedure:**
       a. Navigate to the incidents list page
       b. Locate an incident in the table
       c. Click the "Export" button on that incident's row
       d. Observe: Loading spinner appears for 1-3 seconds
       e. Observe: Browser download dialog opens (or file downloads automatically)
       f. Check: Downloaded file is named `incident-{id}.pdf`
       g. Check: PDF opens in PDF reader (is valid binary)
       h. Check: PDF contains incident title page and metadata (from Phase 10 templates)
    
    3. **Error test:**
       a. Try to export an incident with an invalid ID (modify URL directly if needed)
       b. Observe: Error toast appears with message (if export button disabled during error)
       c. Observe: Button is re-enabled after error
    
    4. **Edge cases:**
       a. Click export button twice quickly
       b. Observe: Button remains disabled, no double request sent
       c. Observe: Only one download triggered
  </action>
  <verify>
    - Manual test: Export button shows loading spinner during PDF generation
    - Manual test: PDF file downloads after 1-3 seconds
    - Manual test: Downloaded PDF is valid and contains incident data
    - Manual test: Error toast appears if export fails
    - Manual test: Button re-enables after success or error
  </verify>
  <done>Local export flow verified — ready for integration tests in Plan 03</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Button Click | Untrusted user action (could click export multiple times) |
| Component → API Route | API key is sent in header (must be kept secure) |
| Browser → File System | Download is user-initiated; browser controls destination |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02a | **S**poofing | API key retrieval | Mitigate | Use same API key retrieval pattern as useIncident hook (localStorage fallback to env); verify in Phase 13 deployment plan |
| T-15-02b | **T**ampering | Download filename | Accept | Filename is generated server-side (route.ts); client-side download link uses safe filename pattern `incident-{id}.pdf` |
| T-15-02c | **D**enial of Service | Double-submit prevention | Mitigate | setExportingId(id) disables button during request; only one fetch in flight per incident |
| T-15-02d | **I**nformation Disclosure | PDF contents in blob | Mitigate | PDF already protected by API key validation in route; blob is temporary and revoked after download |

</threat_model>

<verification>
**Wave 1 Completion Check (after Plan 02):**

Code verification:
1. IncidentList.handleExportClick() is no longer a stub (no console.log)
2. handleExportClick calls fetch() to `/api/incidents/:id/export/pdf`
3. X-API-Key header is included in fetch options
4. response.blob() is used to get binary data
5. Browser download is triggered via createObjectURL + link.click()
6. Loading state is tracked in exportingId state
7. IncidentTable export button is disabled during request
8. Error handling with showErrorToast is in place

Manual testing (see Task 4 verification above):
- Export button shows loading spinner
- PDF downloads after 1-3 seconds
- Downloaded file is valid PDF with incident data
- Error toast appears on failure
- Button re-enables after success/error
- No double-submit when button clicked twice quickly
</verification>

<success_criteria>
- [ ] `IncidentList.handleExportClick()` replaced with actual API call (no console.log)
- [ ] `handleExportClick` calls `GET /api/incidents/:id/export/pdf` with X-API-Key header
- [ ] `handleExportClick` uses `response.blob()` to get binary PDF data
- [ ] Browser download is triggered (blob URL → download link → click → revoke)
- [ ] `exportingId` state tracks which incident is currently exporting
- [ ] `IncidentTable` export button is disabled while `exportingId === id`
- [ ] `IncidentTable` export button shows loading spinner during export
- [ ] `showSuccessToast()` called on successful export
- [ ] `showErrorToast()` called with error message on failure
- [ ] Error handling uses try/catch/finally pattern
- [ ] Button is re-enabled in finally block (even if error occurs)
- [ ] IncidentTable prop receives `isExporting` or `isExportingId` from IncidentList
- [ ] No double-submit possible (button disabled during request)

</success_criteria>

<output>
After completion, create `.planning/phases/15-pdf-export-app-router/15-02-SUMMARY.md` documenting:
- IncidentList.handleExportClick implementation
- API call details and authentication
- Browser download mechanism
- Loading state management
- Error handling strategy
- Next steps: Plan 03 (wire Step6 button + integration tests)
</output>
