---
phase: 01-project-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/layout.tsx
  - src/components/Header.tsx
  - src/components/Footer.tsx
  - src/app/page.tsx
autonomous: true
requirements: [NF2.5, NF2.2, NF2.4]

must_haves:
  truths:
    - "Header zeigt SIAG Logo-Placeholder und Titel 'Incident Management Assistent'"
    - "Footer zeigt 'SIAG Consulting AG'"
    - "Page-Shell hat max-w-4xl zentrierten Content-Bereich"
    - "Build laeuft weiterhin ohne Fehler"
  artifacts:
    - path: "src/components/Header.tsx"
      provides: "SIAG-branded Header"
      contains: "Incident Management Assistent"
    - path: "src/components/Footer.tsx"
      provides: "Minimal footer"
      contains: "SIAG Consulting AG"
    - path: "src/app/layout.tsx"
      provides: "Root layout mit Header + Footer"
      contains: "Header"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/Header.tsx"
      via: "import"
      pattern: "import.*Header"
    - from: "src/app/layout.tsx"
      to: "src/components/Footer.tsx"
      via: "import"
      pattern: "import.*Footer"
---

<objective>
Base Layout mit SIAG-branded Header, Footer und Page-Shell erstellen.

Purpose: Visuelle Huelle fuer den Wizard. Jede spaetere Page erbt Header/Footer automatisch via layout.tsx.
Output: Header.tsx, Footer.tsx, erweitertes layout.tsx mit SIAG-Branding.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-project-foundation/01-RESEARCH.md
@.planning/phases/01-project-foundation/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Design tokens from globals.css (created by Plan 01) -->
Tailwind v4 @theme Tokens available as utility classes:
- bg-navy, text-navy, bg-navy-light, bg-navy-dark
- bg-amber, text-amber, bg-amber-light, bg-amber-dark
- bg-lightgray, text-lightgray
- bg-white, text-white
- font-sans (Inter, system-ui fallback)

Custom properties also usable via var():
- var(--color-navy), var(--color-navy-light), var(--color-navy-dark)
- var(--color-amber), var(--color-amber-light), var(--color-amber-dark)
- var(--color-lightgray), var(--color-white)
- var(--font-sans)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Header- und Footer-Komponenten erstellen</name>
  <files>src/components/Header.tsx, src/components/Footer.tsx</files>
  <read_first>
    src/app/globals.css
    .planning/phases/01-project-foundation/01-RESEARCH.md
  </read_first>
  <action>
1. Verzeichnis `src/components/` erstellen falls nicht vorhanden.

2. `src/components/Header.tsx` erstellen:
   ```tsx
   export function Header() {
     return (
       <header className="bg-navy text-white px-6 py-4">
         <div className="max-w-4xl mx-auto flex items-center gap-4">
           <div className="w-10 h-10 bg-white/20 rounded flex items-center justify-center text-sm font-bold">
             SIAG
           </div>
           <h1 className="text-lg font-semibold">Incident Management Assistent</h1>
         </div>
       </header>
     );
   }
   ```
   HINWEIS: Logo ist ein Placeholder-Div (kein next/image). Wird spaeter durch SVG ersetzt.
   WICHTIG: Kein `'use client'` noetig — rein statische Komponente.
   WICHTIG: Kein `next/*` Import — Plattform-Kompatibilitaet (NF5.1).
   Farben: bg-navy (#1a2e4a) aus @theme{}, text-white. Max-Width 4xl wie Content-Bereich.

3. `src/components/Footer.tsx` erstellen:
   ```tsx
   export function Footer() {
     return (
       <footer className="bg-lightgray text-navy/60 text-sm px-6 py-4 text-center">
         <div className="max-w-4xl mx-auto">
           SIAG Consulting AG
         </div>
       </footer>
     );
   }
   ```
   Farben: bg-lightgray (#f5f7fa), text-navy mit 60% Opacity. Dezent, nicht dominant.
  </action>
  <verify>
    <automated>test -f src/components/Header.tsx && test -f src/components/Footer.tsx && echo "Components exist"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/Header.tsx existiert
    - src/components/Header.tsx enthaelt `bg-navy`
    - src/components/Header.tsx enthaelt `Incident Management Assistent`
    - src/components/Header.tsx enthaelt `SIAG`
    - src/components/Header.tsx enthaelt NICHT `import.*from.*next/`
    - src/components/Footer.tsx existiert
    - src/components/Footer.tsx enthaelt `SIAG Consulting AG`
    - src/components/Footer.tsx enthaelt `bg-lightgray`
    - src/components/Footer.tsx enthaelt NICHT `import.*from.*next/`
  </acceptance_criteria>
  <done>Header und Footer als eigenstaendige Komponenten erstellt, SIAG-Branding angewendet, keine next/* Imports.</done>
</task>

<task type="auto">
  <name>Task 2: Layout.tsx mit Header/Footer und Page-Shell erweitern</name>
  <files>src/app/layout.tsx, src/app/page.tsx</files>
  <read_first>
    src/app/layout.tsx
    src/components/Header.tsx
    src/components/Footer.tsx
  </read_first>
  <action>
1. `src/app/layout.tsx` erweitern — Header und Footer einbinden, Page-Shell mit Flex-Layout:
   ```tsx
   import type { Metadata } from "next";
   import "./globals.css";
   import { Header } from "@/components/Header";
   import { Footer } from "@/components/Footer";

   export const metadata: Metadata = {
     title: "SIAG Incident Management Assistent",
     description: "Incident-Response-Wizard fuer SIAG-Berater",
   };

   export default function RootLayout({
     children,
   }: {
     children: React.ReactNode;
   }) {
     return (
       <html lang="de">
         <body className="min-h-screen flex flex-col bg-white text-navy">
           <Header />
           <main className="flex-1 max-w-4xl mx-auto w-full px-6 py-8">
             {children}
           </main>
           <Footer />
         </body>
       </html>
     );
   }
   ```
   Layout: body ist min-h-screen flex-col, main ist flex-1 (drueckt Footer nach unten).
   Content max-w-4xl zentriert mit px-6 Padding.

2. `src/app/page.tsx` aktualisieren — Placeholder mit SIAG-Styling:
   ```tsx
   export default function Home() {
     return (
       <div className="flex items-center justify-center min-h-[60vh]">
         <div className="text-center">
           <h1 className="text-3xl font-bold text-navy mb-4">
             SIAG Incident Management Assistent
           </h1>
           <p className="text-navy/60 mb-6">
             Wizard wird in Phase 2 implementiert.
           </p>
           <div className="inline-block bg-amber/10 text-amber-dark px-4 py-2 rounded text-sm">
             Foundation Phase — Shell bereit
           </div>
         </div>
       </div>
     );
   }
   ```

3. Build ausfuehren: `npm run build && npm run lint`
   - Muss ohne Fehler durchlaufen
   - `out/` Verzeichnis muss existieren
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && npm run lint 2>&1 | tail -3 && echo "BUILD + LINT OK"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/layout.tsx enthaelt `import { Header } from "@/components/Header"`
    - src/app/layout.tsx enthaelt `import { Footer } from "@/components/Footer"`
    - src/app/layout.tsx enthaelt `<Header />`
    - src/app/layout.tsx enthaelt `<Footer />`
    - src/app/layout.tsx enthaelt `min-h-screen flex flex-col`
    - src/app/layout.tsx enthaelt `max-w-4xl mx-auto`
    - src/app/layout.tsx enthaelt `lang="de"`
    - src/app/page.tsx enthaelt `SIAG Incident Management Assistent`
    - `npm run build` exit code 0
    - `npm run lint` exit code 0
    - Verzeichnis `out/` existiert nach Build
  </acceptance_criteria>
  <done>Layout.tsx bindet Header und Footer ein. Page-Shell mit Flex-Layout. Build und Lint grueen.</done>
</task>

</tasks>

<verification>
- `npm run build` laeuft ohne Fehler
- `npm run lint` laeuft ohne Fehler
- `out/` Verzeichnis existiert
- Header.tsx und Footer.tsx existieren unter src/components/
- layout.tsx importiert Header und Footer
- Keine next/* Imports in Header.tsx oder Footer.tsx
</verification>

<success_criteria>
- SIAG-branded Header mit Logo-Placeholder und Titel sichtbar
- Footer mit "SIAG Consulting AG" am Seitenende
- Sticky Footer (flexbox min-h-screen Layout)
- Build und Lint ohne Fehler
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-02-SUMMARY.md`
</output>
