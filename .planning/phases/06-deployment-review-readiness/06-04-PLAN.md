---
phase: 06-deployment-review-readiness
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - NF5.1

must_haves:
  truths:
    - "README.md exists at project root and is readable without prior context"
    - "README contains a live Vercel URL placeholder or actual URL"
    - "README contains a step-by-step walkthrough (ca. 5-8 minutes)"
    - "README contains 5 specific feedback questions for the consultant"
    - "README contains technical context (no backend, localStorage only, 74 tests green)"
  artifacts:
    - path: "README.md"
      provides: "Consultant-facing review document"
      contains: "Feedback-Fragen"
  key_links:
    - from: "README.md"
      to: "Vercel production URL"
      via: "Live-URL section"
      pattern: "vercel.app"
---

<objective>
Create `README.md` at the project root — a short, action-oriented document for the SIAG consultant. Contains the Vercel URL, wizard walkthrough steps, 5 targeted feedback questions, and minimal technical context.

Purpose: The consultant needs to know what to click, what to evaluate, and what questions to answer — without needing to understand the codebase. This plan runs in Wave 1 parallel to Plan 01 (build fix) because it has no technical dependencies.
Output: README.md at project root, ready for consultant review.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-deployment-review-readiness/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README.md for consultant review</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/06-deployment-review-readiness/06-RESEARCH.md — lines 261–297 (README structure recommendation)
    - .planning/STATE.md — lines 38–65 (Key Decisions for technical context section)
  </read_first>
  <action>
    Check if README.md already exists at the project root:
    ```
    ls README.md 2>/dev/null
    ```

    If it exists, read it first before overwriting. If it does not exist, create it fresh.

    Create (or overwrite) README.md with the following content. Use `{PRODUCTION_URL}` as a placeholder — Plan 02 will produce the actual URL. The executor for Plan 04 does NOT need to wait for the deploy; the README can use the placeholder and be updated after Plan 02 completes.

    ```markdown
    # SIAG Incident Management Assistent — Berater-Review

    ## Live-URL

    **{PRODUCTION_URL}**

    _(URL wird nach erstem Deploy auf Vercel aktualisiert)_

    ---

    ## Was ist das?

    Ein klickbarer 7-Screen Prototyp für einen Incident-Response-Wizard, der SIAG-Berater durch einen Ransomware-Vorfall führt. Der Wizard deckt den vollständigen Ablauf ab: von der Ersterfassung über die Klassifikation und das Reaktions-Playbook bis zur Dokumentation und Übergabe.

    Erstellt im Rahmen des HDT-Workshops «Gruppe Incident Management», Phase 4 Prototyp.

    ---

    ## Workshop-Kontext

    - **Projekt:** Kundenplattform-Prototyp für SIAG-Incident-Response-Services
    - **Ziel:** Validierung des Wizard-Flows und der inhaltlichen Vollständigkeit vor Integration in die Kundenplattform
    - **Scope:** Frontend-Prototyp (kein Backend, alle Daten bleiben im Browser)

    ---

    ## Durchlauf (ca. 5–8 Minuten)

    Empfehlung: Einen **Kritisch-Szenario** durchspielen (alle 3 Klassifikationsfragen mit «Ja» beantworten).

    1. **Screen 0 — No-Go-Regeln:** Checkbox bestätigen, «Weiter» klicken
    2. **Screen 1 — Einstieg:** «Shit Happens» Button klicken, «Weiter» klicken
    3. **Screen 2 — Vorfall erfassen:** «Jetzt» Button klicken (setzt Zeitstempel), Systeme auswählen, «Ransomware» ankreuzen
    4. **Screen 3 — Klassifikation:** Alle 3 Fragen mit «Ja» beantworten → Schweregrad «Kritisch» erscheint automatisch
    5. **Screen 4 — Reaktionsschritte:** 2–3 Massnahmen abhaken, Fortschritts-Zähler beobachten
    6. **Screen 5 — Meldepflichten:** Fragen beantworten → ISG/DSG/FINMA-Fristen erscheinen; Kommunikationsbausteine öffnen
    7. **Screen 6 — Zusammenfassung:** Alle Eingaben konsolidiert sichtbar; «Bericht exportieren (PDF)» testen; «An SIAG-Berater übergeben»-Button sehen

    ---

    ## Feedback-Fragen

    1. **Vollständigkeit des Flows:** Fehlt ein inhaltlicher Schritt im Wizard-Ablauf, der für einen echten Incident relevant wäre?
    2. **Meldepflichten:** Sind die dargestellten Fristen und Behörden (ISG 24h, DSG, FINMA 24h/72h) korrekt und vollständig für den CH-Kontext?
    3. **Kommunikationsbausteine:** Sind die Textvorlagen für GL, Mitarbeitende und Medien praxistauglich? Was würde ein Berater sofort ändern?
    4. **Kundenpräsentation:** Was würde diesen Prototyp heute für eine Kundenpräsentation blockieren?
    5. **Freies Feedback:** Was fällt noch auf — inhaltlich, sprachlich oder konzeptionell?

    ---

    ## Technischer Stand

    | Eigenschaft | Wert |
    |------------|------|
    | Framework | Next.js 16, TypeScript, Tailwind v4 |
    | Datenspeicherung | localStorage (Browser only — kein Backend, keine Datenübertragung) |
    | Tests | 74/74 grün (Vitest) |
    | Deployment | Vercel Static Export (CDN, kein Server) |
    | Mobile | Getestet auf 375px Viewport |

    ---

    ## Bekannte Einschränkungen (Out of Scope für diesen Prototyp)

    - Kein echtes Backend / keine Datenpersistenz über Browser hinaus
    - PDF-Export via Browser-Druckdialog (kein dediziertes PDF-Library)
    - Logo ist ein Placeholder (SVG-Vorlage, kein finales SIAG-Logo)
    - Kein Authentication / Multi-User

    ---

    _Fragen oder Feedback direkt an pascalammeter (GitHub: pascalammeter/siag-incident-assistant)_
    ```
  </action>
  <verify>
    <automated>test -f README.md && grep -c "Feedback-Fragen" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f README.md` exits 0
    - `grep "Feedback-Fragen" README.md` returns a match
    - `grep "vercel.app\|PRODUCTION_URL" README.md` returns a match (URL placeholder present)
    - `grep "localStorage" README.md` returns a match (data privacy note present)
    - `grep "74/74" README.md` returns a match (test status noted)
    - File is human-readable in German (not English)
  </acceptance_criteria>
  <done>README.md exists at project root with all 6 sections: URL, description, workshop context, walkthrough, 5 feedback questions, technical context</done>
</task>

</tasks>

<verification>
After task completion:
- `test -f README.md` — file exists
- `grep -c "##" README.md` — returns 6 or more (all sections present)
- `grep "Feedback-Fragen" README.md` — section header present
- `grep "PRODUCTION_URL\|vercel.app" README.md` — URL present (placeholder or actual)

Note: After Plan 02 completes and the actual Vercel URL is known, the executor for Plan 02 (or Plan 04 wrap-up) should replace `{PRODUCTION_URL}` with the actual URL.
</verification>

<success_criteria>
README.md exists, is readable in German, contains the production URL (or placeholder), 5 feedback questions, and technical context. A SIAG consultant with no prior context can start the wizard within 60 seconds of reading it.
</success_criteria>

<output>
After completion, create `.planning/phases/06-deployment-review-readiness/06-04-SUMMARY.md`

Note for the executor: If Plan 02 has already completed by the time this plan runs (possible since both are in Wave 1), read 06-02-SUMMARY.md and replace `{PRODUCTION_URL}` in README.md with the confirmed actual URL before creating this SUMMARY.
</output>
