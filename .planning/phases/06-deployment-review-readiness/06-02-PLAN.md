---
phase: 06-deployment-review-readiness
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified: []
autonomous: false
requirements:
  - NF4.4

must_haves:
  truths:
    - "A public Vercel URL exists and is reachable in a browser"
    - "The production build on Vercel shows all 7 wizard screens"
    - "The Vercel dashboard shows deployment status as Ready"
  artifacts:
    - path: ".vercel/project.json"
      provides: "Project link to Vercel (already exists — no changes needed)"
      contains: "projectId"
  key_links:
    - from: "git push origin main"
      to: "Vercel GitHub webhook"
      via: "GitHub→Vercel integration (already configured)"
      pattern: "push.*main"
---

<objective>
Push the clean build to `main` on GitHub, which triggers the existing Vercel GitHub integration and produces a live production URL. Human confirms deployment succeeded in the Vercel dashboard.

Purpose: The public Vercel URL is the deliverable for consultant review. No CLI tooling is needed — the GitHub→Vercel integration is already active (`.vercel/project.json` with `prj_F51utPzXBnLNyGi8YngZHXMy2Sqv` is present, GitHub remote `pascalammeter/siag-incident-assistant` is confirmed).
Output: Live production URL (expected: `https://siag-incident-assistant.vercel.app`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-deployment-review-readiness/06-RESEARCH.md
@.planning/phases/06-deployment-review-readiness/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit and push to main</name>
  <files></files>
  <read_first>
    - .planning/phases/06-deployment-review-readiness/06-01-SUMMARY.md — confirm build was clean before pushing
    - .vercel/project.json — confirm projectId is present (do not modify)
  </read_first>
  <action>
    Run the following sequence:

    1. Check current git status:
    ```
    git status
    git log --oneline -5
    ```

    2. Stage all changed files from Phase 6 work so far (next.config.ts and any other uncommitted changes):
    ```
    git add next.config.ts
    git add -p   # review any other staged files
    ```

    Or if using gsd-tools commit:
    ```
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "feat(06): silence turbopack workspace-root warning" --files next.config.ts
    ```

    3. Push to main:
    ```
    git push origin main
    ```

    Expected: Push succeeds. GitHub webhook fires automatically. Do NOT use Vercel CLI (`vercel --prod`) — the GitHub integration is the correct path and avoids creating a second pipeline.

    After push, capture the latest commit SHA for the SUMMARY.
  </action>
  <verify>
    <automated>git log --oneline -3 2>&1 && git status 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `git status` shows "nothing to commit, working tree clean"
    - `git log --oneline -1` shows the commit message for the next.config.ts change
    - `git remote get-url origin` returns `https://github.com/pascalammeter/siag-incident-assistant.git`
  </acceptance_criteria>
  <done>next.config.ts change committed and pushed to origin/main</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Vercel deployment is live</name>
  <what-built>
    A commit was pushed to main on GitHub. The existing Vercel GitHub integration should have triggered an automatic production deployment. No Vercel CLI was used.
  </what-built>
  <how-to-verify>
    1. Open https://vercel.com/dashboard (log in with the GitHub account `pascalammeter`)
    2. Find the project `siag-incident-assistant`
    3. Confirm the latest deployment shows status "Ready" (green checkmark)
    4. Copy the production URL — it should be `https://siag-incident-assistant.vercel.app` or similar
    5. Open the URL in a browser — confirm the SIAG wizard loads (Screen 0 visible with 8 No-Go rules)
    6. Note the exact production URL for use in Plan 04 (README.md)

    If deployment is still in-progress: wait 60–90 seconds and refresh.
    If deployment failed: check the Vercel build logs for the error and report it here.
  </how-to-verify>
  <resume-signal>
    Type the production URL (e.g. "https://siag-incident-assistant.vercel.app") if deployment succeeded, or describe the error if it failed.
  </resume-signal>
</task>

</tasks>

<verification>
After human confirmation:
- Production URL provided and accessible in browser
- Vercel dashboard shows deployment status "Ready"
- Screen 0 (No-Go Interstitial) loads correctly at the production URL
</verification>

<success_criteria>
A public Vercel URL is reachable, shows the wizard's Screen 0, and the Vercel dashboard confirms deployment status "Ready".
</success_criteria>

<output>
After completion, create `.planning/phases/06-deployment-review-readiness/06-02-SUMMARY.md`
Include the confirmed production URL in the SUMMARY.
</output>
