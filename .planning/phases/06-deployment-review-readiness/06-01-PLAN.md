---
phase: 06-deployment-review-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
autonomous: true
requirements:
  - NF3.1
  - NF4.1

must_haves:
  truths:
    - "`npm run build` completes without warnings or errors"
    - "The `out/` directory is produced with all static pages"
    - "TypeScript compilation produces no errors"
    - "All 74 vitest tests remain green after the config change"
  artifacts:
    - path: "next.config.ts"
      provides: "Build configuration with turbopack.root silencing workspace warning"
      contains: "turbopack"
    - path: "out/"
      provides: "Static export output directory"
  key_links:
    - from: "next.config.ts"
      to: "npm run build"
      via: "turbopack.root: __dirname"
      pattern: "turbopack"
---

<objective>
Add `turbopack: { root: __dirname }` to next.config.ts to silence the workspace-root detection warning, then verify `npm run build` produces a clean `out/` with zero warnings and all 74 tests still pass.

Purpose: A clean build output is the prerequisite for the Vercel production deploy (Plan 02). The warning itself is cosmetic but must be silenced before the build output is considered reviewer-ready.
Output: next.config.ts updated, clean `out/` directory, confirmed test suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-deployment-review-readiness/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add turbopack.root to next.config.ts</name>
  <files>next.config.ts</files>
  <read_first>
    - next.config.ts — read the current config before modifying
    - .planning/phases/06-deployment-review-readiness/06-RESEARCH.md — lines 88–127 (turbopack fix options)
  </read_first>
  <action>
    Read next.config.ts (currently 10 lines). Replace the entire file content with the following — use the top-level `turbopack` key (NOT `experimental.turbo`), which is the stable path in Next.js 16:

    ```typescript
    import type { NextConfig } from "next";

    const nextConfig: NextConfig = {
      output: "export",
      images: {
        unoptimized: true,
      },
      turbopack: {
        root: __dirname,
      },
    };

    export default nextConfig;
    ```

    Note: `__dirname` is a Node.js global available in .ts config files processed by Next.js. No `path` import is needed.

    If TypeScript raises a type error on `turbopack.root`, fall back to:
    ```typescript
    experimental: {
      turbo: {
        root: __dirname,
      },
    },
    ```
    and note which worked in the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "turbopack" next.config.ts` returns a match
    - File still contains `output: "export"` (not removed)
    - File still contains `images: { unoptimized: true }` (not removed)
  </acceptance_criteria>
  <done>next.config.ts contains the turbopack root config alongside the existing output and images settings</done>
</task>

<task type="auto">
  <name>Task 2: Run production build and confirm clean output</name>
  <files></files>
  <read_first>
    - next.config.ts — confirm Task 1 changes are present before running build
  </read_first>
  <action>
    Run the full production build:
    ```
    npm run build 2>&1
    ```

    Expected: Build completes with exit code 0. The workspace-root warning should be gone. The `out/` directory is created.

    Then run the full test suite to confirm nothing regressed:
    ```
    npx vitest run
    ```

    Expected: 74 passing tests, 0 failures.

    If the build fails due to the `turbopack.root` key causing a TypeScript error, apply the `experimental.turbo` fallback documented in Task 1 and rebuild.

    Capture the last 20 lines of build output for the SUMMARY.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20 && npx vitest run --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - `ls out/` returns a non-empty directory listing (index.html present)
    - Build output does NOT contain "inferred your workspace root" warning
    - Vitest output contains "74 passed" (or more if new tests were added)
  </acceptance_criteria>
  <done>Clean build: out/ produced, workspace-root warning absent, 74/74 tests green</done>
</task>

</tasks>

<verification>
After both tasks:
- `grep -n "turbopack" next.config.ts` — config key present
- `npm run build 2>&1 | grep -i "workspace root"` — returns nothing (warning silenced)
- `ls out/index.html` — static export produced
- `npx vitest run --reporter=dot 2>&1 | tail -3` — all tests pass
</verification>

<success_criteria>
`npm run build` completes silently (no workspace-root warning), exits 0, produces `out/`, and `npx vitest run` shows 74 passed tests.
</success_criteria>

<output>
After completion, create `.planning/phases/06-deployment-review-readiness/06-01-SUMMARY.md`
</output>
