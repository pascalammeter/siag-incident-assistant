---
phase: 17-cicd-swagger-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/deploy.yml
  - .github/workflows/README.md
autonomous: true
requirements:
  - DE3.1
  - DE3.2
  - DE3.3
  - DE3.4
must_haves:
  truths:
    - ".github/workflows/ci.yml exists and runs npm test on pull requests"
    - "Merge is blocked if npm test fails (branch protection rule enforced)"
    - ".github/workflows/deploy.yml triggers on push to main and deploys to Vercel"
    - "GitHub Actions CI status visible in PR checks (required for merge)"
    - "Deploy workflow automatically triggers Vercel deployment"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI workflow that runs tests on every PR"
      exports: ["on: pull_request", "npm test"]
    - path: ".github/workflows/deploy.yml"
      provides: "Deploy workflow that triggers Vercel on main branch push"
      exports: ["on: push (main)", "vercel deploy"]
    - path: ".github/workflows/README.md"
      provides: "Documentation for CI/CD workflow configuration"
      exports: ["Branch protection setup instructions", "Vercel deployment settings"]
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "npm test"
      via: "GitHub Actions workflow execution"
      pattern: "run: npm test"
    - from: ".github/workflows/deploy.yml"
      to: "Vercel API"
      via: "Vercel CLI or GitHub integration"
      pattern: "vercel deploy|VERCEL_TOKEN"
    - from: "Branch protection rule"
      to: ".github/workflows/ci.yml"
      via: "Require status checks to pass"
      pattern: "checks-required"
---

<objective>
Create GitHub Actions CI/CD workflows for continuous integration testing and deployment.

**Purpose:** Gate merges with passing test suite (CI) and automate production deployment (CD). Closes DE3 CI/CD gap from v1.1 audit. Ensures code quality via automated testing before merge.

**Output:**
1. `.github/workflows/ci.yml` — Runs `npm test` on all pull requests, blocks merge if tests fail
2. `.github/workflows/deploy.yml` — Triggers Vercel deployment on push to main
3. `.github/workflows/README.md` — Setup documentation for branch protection and Vercel integration
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@package.json
@.github

# Test setup from Phase 12
@.planning/phases/12-testing-security/12-01-SUMMARY.md

# GitHub Actions best practices for Next.js
# Reference: https://github.com/vercel/next.js/blob/canary/.github/workflows/test.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions CI Workflow (ci.yml)</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Create `.github/workflows/ci.yml` that runs tests on every pull request to main.

This workflow:
1. Triggers on pull_request to main branch
2. Checks out code
3. Sets up Node.js 20.x (matches package.json engines if specified, or use LTS)
4. Installs dependencies with npm ci (not npm install — faster, more reliable in CI)
5. Runs prisma generate to ensure types are fresh
6. Runs npm test (vitest) with verbose output
7. Reports test results to GitHub UI (PR checks)
8. Blocks merge if tests fail

File location: `.github/workflows/ci.yml`

Content:
```yaml
name: CI — Test Suite

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - develop

jobs:
  test:
    name: Test Suite (Node 20)
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Setup Node.js 20.x
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci
        env:
          CI: true

      - name: Generate Prisma Client
        run: npm run prisma:generate
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL_CI }}

      - name: Run test suite
        run: npm test -- --reporter=verbose
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL_CI }}
          API_KEY: ${{ secrets.API_KEY_CI }}
          NODE_ENV: test

      - name: Upload coverage reports (optional)
        uses: codecov/codecov-action@v3
        if: always()
        with:
          files: ./coverage/coverage-final.json
          fail_ci_if_error: false
          verbose: true
```

Implementation notes:

1. **Triggers:**
   - `on: pull_request: branches: [main]` — Run on every PR to main
   - `on: push: branches: [develop]` — Optional: run on develop pushes for feedback

2. **Node version:**
   - Use 20.x (LTS as of 2026)

3. **Dependencies:**
   - Use `npm ci` instead of `npm install` (faster, locked to package-lock.json)

4. **Prisma:**
   - Run `prisma generate` before tests (ensures Prisma Client is current)

5. **Secrets required in GitHub repo:**
   - DATABASE_URL_CI (Neon test database URL)
   - API_KEY_CI (test API key, e.g., "test-key-32-chars-minimum-here")

  </action>
  <verify>
    - Confirm `.github/workflows/ci.yml` exists
    - Confirm `on: pull_request` trigger is present
    - Confirm `npm ci` is used
    - Confirm `npm test` is called
    - Confirm DATABASE_URL and API_KEY are from secrets
    - Confirm timeout-minutes is 15
  </verify>
  <done>CI workflow file created</done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub Actions Deploy Workflow (deploy.yml)</name>
  <files>.github/workflows/deploy.yml</files>
  <action>
Create `.github/workflows/deploy.yml` that triggers Vercel deployment on push to main.

File location: `.github/workflows/deploy.yml`

Content:
```yaml
name: Deploy to Vercel (Production)

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy to Vercel
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment:
      name: production
      url: https://siag-incident-assistant.vercel.app

    steps:
      - uses: actions/checkout@v4

      - name: Deploy to Vercel
        uses: vercel/action@v4
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          production: true
```

Implementation notes:

1. **Triggers:**
   - `on: push: branches: [main]` — Deploy on every push to main
   - `workflow_dispatch:` — Allow manual deployments

2. **Vercel integration:**
   - Use official `vercel/action` (simplest, maintained by Vercel)

3. **Secrets required:**
   - VERCEL_TOKEN: Get from Vercel Settings → Tokens
   - VERCEL_ORG_ID: Get from Vercel Project Settings
   - VERCEL_PROJECT_ID: Get from Vercel Project Settings

  </action>
  <verify>
    - Confirm `.github/workflows/deploy.yml` exists
    - Confirm `on: push: branches: [main]` trigger is present
    - Confirm vercel/action is used
    - Confirm secrets are referenced
    - Confirm production: true is set
  </verify>
  <done>Deploy workflow file created</done>
</task>

<task type="auto">
  <name>Task 3: Create .github/workflows/README.md with Setup Instructions</name>
  <files>.github/workflows/README.md</files>
  <action>
Create `.github/workflows/README.md` that documents CI/CD setup and configuration.

File location: `.github/workflows/README.md`

Content:
```markdown
# GitHub Actions CI/CD Workflows

This directory contains GitHub Actions workflows for continuous integration and deployment.

## Workflows

### ci.yml — Continuous Integration (Test Suite)

**Trigger:** Pull requests to `main` branch

**What it does:**
1. Checks out code
2. Sets up Node.js 20.x
3. Installs dependencies with `npm ci`
4. Generates Prisma Client
5. Runs test suite with `npm test`
6. Uploads coverage reports (optional)

**Status:** Required check before merge (blocks merge if fails)

**Secrets required:**
- `DATABASE_URL_CI` — Test database URL (Neon test instance)
- `API_KEY_CI` — Test API key (≥32 characters)

### deploy.yml — Continuous Deployment (Vercel)

**Trigger:** Push to `main` branch (after CI passes)

**What it does:**
1. Checks out code
2. Deploys to Vercel production using official `vercel/action`
3. Sets environment variables from Vercel dashboard
4. Completes deployment

**Status:** Automatic after CI passes

**Secrets required:**
- `VERCEL_TOKEN` — Personal access token from Vercel
- `VERCEL_ORG_ID` — Vercel organization ID
- `VERCEL_PROJECT_ID` — Vercel project ID

## Setup Instructions

### 1. Configure GitHub Secrets

Go to: Repository Settings → Secrets and variables → Actions

**Add these secrets:**

#### For CI Workflow
- **DATABASE_URL_CI**: Neon test database connection string
  - Format: `postgresql://user:password@host/database?...`
  - Get from: Neon Dashboard → Project → Connection String (test database)

- **API_KEY_CI**: Test API key for automated testing
  - Generate: Any string ≥32 characters (e.g., `test-key-1234567890123456789012`)
  - Must match the test environment value

#### For Deploy Workflow
- **VERCEL_TOKEN**: Personal access token for Vercel CLI
  - Get from: Vercel Dashboard → Settings → Tokens → Create Token
  - Scope: Full Access

- **VERCEL_ORG_ID**: Vercel organization ID
  - Get from: Vercel Dashboard → Settings → General → Organization ID
  - Example: `team_abc123def456`

- **VERCEL_PROJECT_ID**: Vercel project ID
  - Get from: Vercel Dashboard → Project Settings → Project ID
  - Example: `prj_xyz789uvw123`

### 2. Configure Branch Protection (Enforce CI)

Go to: Repository Settings → Branches → main → Add rule

**Settings:**
- Branch name pattern: `main`
- Require status checks to pass before merging: **✓**
- Require status checks: Select `CI — Test Suite`
- Require branches to be up to date before merging: **✓**
- Require code reviews before merging: **✓** (optional, recommended)
- Require approval from code owners: ✓ (optional)
- Dismiss stale pull request approvals: ✓ (optional)

**Result:**
- CI must pass before merge
- Red ✗ if tests fail
- Green ✓ if tests pass

### 3. Verify Deployment Configuration

In Vercel Dashboard:

1. **Environment Variables:**
   - Go to Project Settings → Environment Variables
   - Ensure all required env vars are set:
     - `DATABASE_URL` (production Neon database)
     - `API_KEY` (production API key)
     - `NEXT_PUBLIC_*` variables (if any)
   - Deploy on main should automatically pick these up

2. **Deployment Trigger:**
   - Go to Project Settings → Git
   - Production Branch: `main`
   - Framework Preset: `Next.js`
   - Root Directory: `.` (project root)
   - Build Command: (leave default or use `npm run build`)

## Workflow Status

View workflow status in GitHub:

1. **PR Checks:**
   - Open any PR to main
   - Scroll down to "Checks" section
   - See `CI — Test Suite` status (pending, running, passed, failed)

2. **Deployment:**
   - Go to Deployments tab in repository
   - See deployment history and status
   - View Vercel deployment logs

## Manual Triggers

### Run CI manually (for testing)
```bash
# Trigger workflow from GitHub Actions tab
# Click "Run workflow" on ci.yml
```

### Deploy manually (if needed)
```bash
# Trigger workflow from GitHub Actions tab
# Click "Run workflow" on deploy.yml
# Or use: gh workflow run deploy.yml --ref main
```

## Troubleshooting

### CI fails: "DATABASE_URL_CI not found"
- Add the secret to GitHub: Settings → Secrets → Add `DATABASE_URL_CI`

### CI fails: "Prisma Client generation failed"
- Check that test database is accessible
- Verify DATABASE_URL_CI is correct connection string

### CI fails: "npm test failed"
- Check test output in workflow logs
- Fix failing tests locally: `npm test`
- Push fix and re-run workflow

### Deploy fails: "VERCEL_TOKEN invalid"
- Generate new token in Vercel: Settings → Tokens → Create Token
- Update GitHub secret: Settings → Secrets → Update `VERCEL_TOKEN`

### Deploy fails: "Project not found"
- Verify VERCEL_ORG_ID and VERCEL_PROJECT_ID are correct
- Get values from Vercel Dashboard → Project Settings

## References

- [GitHub Actions documentation](https://docs.github.com/en/actions)
- [Vercel GitHub Action](https://github.com/vercel/action)
- [Next.js CI/CD example](https://nextjs.org/docs/app/guides/testing#continuous-integration)
- [Neon connection strings](https://neon.tech/docs/connect/connection-strings)
```

  </action>
  <verify>
    - Confirm `.github/workflows/README.md` exists
    - Confirm README documents both ci.yml and deploy.yml workflows
    - Confirm README includes setup instructions for secrets
    - Confirm README includes branch protection configuration steps
    - Confirm README includes troubleshooting guide
    - Confirm README has clear section for DATABASE_URL_CI and VERCEL_TOKEN setup
  </verify>
  <done>Workflow documentation created</done>
</task>

<task type="auto">
  <name>Task 4: Configure GitHub Secrets and Branch Protection</name>
  <files>.github/workflows/ci.yml, .github/workflows/deploy.yml</files>
  <action>
Configure GitHub repository secrets and branch protection rules to enforce CI/CD.

Steps:

1. **Add GitHub Secrets:**
   - Go to GitHub repo → Settings → Secrets and variables → Actions
   - Click "New repository secret"
   - Add these secrets:
     * DATABASE_URL_CI: Neon test database URL (ask user or create separate test DB)
     * API_KEY_CI: Test API key string (≥32 chars, e.g., "test-key-1234567890123456789012ab")
     * VERCEL_TOKEN: Get from Vercel Settings → Tokens
     * VERCEL_ORG_ID: Get from Vercel Project Settings
     * VERCEL_PROJECT_ID: Get from Vercel Project Settings

2. **Configure Branch Protection for main:**
   - Go to Settings → Branches
   - Click "Add rule"
   - Branch name pattern: `main`
   - Check: "Require status checks to pass before merging"
   - Select check: `CI — Test Suite` (or `test` depending on workflow name)
   - Check: "Require branches to be up to date before merging"
   - Check: "Require code reviews before merging" (optional, 1 approval)
   - Save rule

3. **Verify CI runs on next PR:**
   - Create a test branch: `git checkout -b test/ci-workflow`
   - Make a small change (e.g., update README)
   - Push: `git push origin test/ci-workflow`
   - Create PR to main
   - Observe: GitHub checks show "CI — Test Suite" as pending
   - Wait for check to complete (pass or fail)

4. **Verify merge is blocked if CI fails:**
   - If CI passes: "Merge pull request" button is enabled
   - If CI fails: "Merge pull request" button is disabled (red X next to check)

5. **Verify deployment on merge:**
   - Once PR is merged to main
   - Go to GitHub Actions tab
   - Observe: `deploy.yml` workflow triggers automatically
   - Watch deployment progress in Vercel
   - Check that production URL is updated: https://siag-incident-assistant.vercel.app

**Note for user:**
This task requires manual action in GitHub and Vercel dashboards. Executor should:
1. Provide user with list of secrets needed (DATABASE_URL_CI, API_KEY_CI, VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID)
2. Walk through GitHub Settings → Secrets configuration
3. Walk through branch protection setup
4. Test with a sample PR to verify CI runs and blocks bad merges
5. Test merge to main to verify deployment triggers

  </action>
  <verify>
    - Confirm DATABASE_URL_CI secret is added to GitHub
    - Confirm API_KEY_CI secret is added
    - Confirm VERCEL_TOKEN secret is added
    - Confirm VERCEL_ORG_ID secret is added
    - Confirm VERCEL_PROJECT_ID secret is added
    - Confirm branch protection rule exists for main branch
    - Confirm CI check is required before merge
    - Confirm PR shows CI check status (pending, running, passed, failed)
    - Confirm bad merge is blocked (red X if CI fails)
    - Confirm deployment triggers on push to main
  </verify>
  <done>GitHub Actions CI/CD fully configured and operational</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub → Action Runners | Trusted: GitHub-hosted runners (ubuntu-latest) |
| Runners → Secrets | Secrets masked in logs; only accessible to workflows |
| CI → Database | Separate test database (DATABASE_URL_CI) for safety |
| Vercel → Production | Secrets protected; deployment tokens scoped to project |
| GitHub → Vercel | Webhook-based integration; HTTPS only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01a | **S**poofing | GitHub Actions | Mitigate | Use official `actions/checkout@v4` and `actions/setup-node@v4` |
| T-17-01b | **T**ampering | Secrets in logs | Mitigate | GitHub masks secrets in logs; rotate tokens regularly |
| T-17-01c | **R**epudiation | CI/CD actions | Mitigate | GitHub Actions logs all workflow runs with timestamps and user info |
| T-17-01d | **I**nformation Disclosure | API_KEY in CI | Mitigate | Use separate test API_KEY_CI (not production key) |
| T-17-01e | **D**enial of Service | CI resource limits | Mitigate | GitHub Actions has quota limits; Vercel deploy has timeout (10 min) |
| T-17-01f | **E**levation of Privilege | Workflow access to repo | Mitigate | Use GITHUB_TOKEN with minimal scopes; never grant write access to main branch |

</threat_model>

<verification>
**Phase 17-01 Completion Check:**

Workflow files:
1. `.github/workflows/ci.yml` exists with pull_request and push triggers
2. `npm ci && npm test` is configured
3. DATABASE_URL_CI and API_KEY_CI from secrets
4. `.github/workflows/deploy.yml` exists with push to main trigger
5. Vercel action configured with token and secrets
6. `.github/workflows/README.md` documents all setup steps

GitHub configuration:
1. DATABASE_URL_CI secret added
2. API_KEY_CI secret added
3. VERCEL_TOKEN secret added
4. VERCEL_ORG_ID secret added
5. VERCEL_PROJECT_ID secret added
6. Branch protection rule on main requires CI to pass
7. CI check is named "CI — Test Suite" (or matches workflow)

Testing:
1. Create test PR → CI runs automatically
2. CI passes → Merge enabled
3. CI fails → Merge blocked (red X)
4. Merge to main → Deploy workflow triggers
5. Deploy completes → Production URL updated
</verification>

<success_criteria>
- [ ] `.github/workflows/ci.yml` exists with pull_request trigger
- [ ] `.github/workflows/ci.yml` runs `npm ci && npm test`
- [ ] `.github/workflows/ci.yml` uses DATABASE_URL_CI and API_KEY_CI from secrets
- [ ] `.github/workflows/ci.yml` timeout set to 15 minutes
- [ ] `.github/workflows/deploy.yml` exists with push to main trigger
- [ ] `.github/workflows/deploy.yml` uses vercel/action
- [ ] `.github/workflows/deploy.yml` has VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID secrets
- [ ] `.github/workflows/deploy.yml` sets production: true
- [ ] `.github/workflows/README.md` documents workflow setup
- [ ] GitHub secrets configured: DATABASE_URL_CI, API_KEY_CI, VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID
- [ ] Branch protection rule configured for main branch
- [ ] CI check required before merge
- [ ] Merge blocked if tests fail (red X visible)
- [ ] Merge enabled if tests pass (green ✓ visible)
- [ ] Deploy workflow triggers on push to main
- [ ] Vercel deployment completes successfully
- [ ] Production URL accessible after deployment

</success_criteria>

<output>
After completion, create `.planning/phases/17-cicd-swagger-polish/17-01-SUMMARY.md` documenting:
- GitHub Actions workflows created (ci.yml, deploy.yml)
- Secrets configured in GitHub
- Branch protection rules enforced
- CI test results and coverage
- Deploy workflow results
- Phase 17-01 completion and production readiness

Also update `.planning/STATE.md`:
- Phase 17-01 complete
- CI/CD workflows operational
- Ready for Phase 17-02 (Swagger endpoint)
</output>
