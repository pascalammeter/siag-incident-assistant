---
phase: 04-screens-4-5-response-communication
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/components/wizard/steps/Step4Reaktion.tsx
autonomous: true
requirements: [F6.1, F6.2, F6.3, F6.4, F6.5]

must_haves:
  truths:
    - "Screen 4 renders 25 checklist items grouped in 4 phase sections"
    - "Each checkbox toggle persists in wizard state via UPDATE_STEP dispatch"
    - "Progress counter shows X von 25 erledigt with progress bar"
    - "No-Go items display amber warning box below the checklist item"
    - "Role badges appear next to each step text"
    - "Weiter button is disabled until completedSteps.length === 25"
    - "Zurueck button always works (per D-04)"
    - "Checked items survive navigation away and back"
  artifacts:
    - path: "src/components/wizard/steps/Step4Reaktion.tsx"
      provides: "Full Screen 4 implementation replacing placeholder"
      min_lines: 80
  key_links:
    - from: "src/components/wizard/steps/Step4Reaktion.tsx"
      to: "src/lib/playbook-data.ts"
      via: "import RANSOMWARE_PLAYBOOK"
      pattern: "RANSOMWARE_PLAYBOOK"
    - from: "src/components/wizard/steps/Step4Reaktion.tsx"
      to: "src/components/wizard/WizardContext.tsx"
      via: "useWizard() for state.reaktion and dispatch UPDATE_STEP"
      pattern: "useWizard"
    - from: "src/components/wizard/steps/Step4Reaktion.tsx"
      to: "src/components/wizard/StepNavigator.tsx"
      via: "manual StepNavigator with isNextDisabled"
      pattern: "StepNavigator"
---

<objective>
Implement Screen 4 (Reaktionsschritte): the 25-point ransomware playbook checklist with phase groups, progress counter, no-go warnings, role badges, and navigation gating.

Purpose: This is the core incident response tool -- a guided, checkable protocol that ensures nothing is missed during a ransomware incident.
Output: Fully implemented Step4Reaktion.tsx replacing the placeholder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-screens-4-5-response-communication/04-CONTEXT.md
@.planning/phases/04-screens-4-5-response-communication/04-UI-SPEC.md
@.planning/phases/04-screens-4-5-response-communication/04-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan depends on -->

From src/lib/playbook-data.ts (created in Plan 01):
```typescript
export interface PlaybookStep {
  id: string
  text: string
  role: 'IT-Leiter' | 'CISO' | 'CEO' | 'Forensik'
  noGoWarning?: string
}
export interface PlaybookPhase {
  id: string
  title: string
  steps: PlaybookStep[]
}
export interface Playbook {
  incidentType: string
  phases: PlaybookPhase[]
}
export const RANSOMWARE_PLAYBOOK: Playbook
```

From src/lib/wizard-types.ts (updated in Plan 01):
```typescript
export interface ReaktionData {
  completedSteps: string[]
}
```

From src/components/wizard/WizardContext.tsx:
```typescript
export function useWizard(): { state: WizardState; dispatch: Dispatch<WizardAction> }
// dispatch({ type: 'UPDATE_STEP', stepKey: 'reaktion', data: { completedSteps: [...] } })
// dispatch({ type: 'NEXT_STEP' })
// dispatch({ type: 'PREV_STEP' })
```

From src/components/wizard/StepNavigator.tsx:
```typescript
interface StepNavigatorProps {
  currentStep: number
  onNext: () => void
  onPrev: () => void
  isNextDisabled?: boolean
  nextLabel?: string
  prevLabel?: string
  showPrev?: boolean
  showNext?: boolean
  nextButtonType?: 'button' | 'submit'
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Step4Reaktion checklist UI</name>
  <files>src/components/wizard/steps/Step4Reaktion.tsx</files>
  <read_first>
    src/components/wizard/steps/Step4Reaktion.tsx,
    src/components/wizard/steps/Step3Klassifikation.tsx,
    src/components/wizard/StepNavigator.tsx,
    src/components/wizard/WizardContext.tsx,
    src/lib/playbook-data.ts,
    src/lib/wizard-types.ts,
    .planning/phases/04-screens-4-5-response-communication/04-UI-SPEC.md
  </read_first>
  <action>
    Replace the entire placeholder Step4Reaktion.tsx with a full implementation.

    **CRITICAL: Screen 4 does NOT use StepForm** (see RESEARCH.md Pitfall 1). StepForm ties navigation to form submission. Screen 4 needs manual checkbox-by-checkbox state updates via `UPDATE_STEP` dispatch, with navigation gated by completion count.

    **Component structure:**
    ```
    Step4Reaktion (outer wrapper)
      -> Step4Content (inner component that uses hooks)
    ```

    Use the inner component pattern from Step3Klassifikation (function inside the file that uses hooks directly).

    **Step4Content implementation:**

    1. `const { state, dispatch } = useWizard()`
    2. Read completedSteps: `const completedSteps: string[] = (state.reaktion as ReaktionData)?.completedSteps ?? []`
    3. Compute total: `const totalSteps = RANSOMWARE_PLAYBOOK.phases.reduce((sum, p) => sum + p.steps.length, 0)`
    4. Compute completedCount: `completedSteps.length`

    **toggleStep function:**
    ```typescript
    const toggleStep = (stepId: string) => {
      const next = completedSteps.includes(stepId)
        ? completedSteps.filter(id => id !== stepId)
        : [...completedSteps, stepId]
      dispatch({ type: 'UPDATE_STEP', stepKey: 'reaktion', data: { completedSteps: next } })
    }
    ```

    **Layout (top to bottom, per UI-SPEC):**

    a) Screen header (S4-01):
    ```html
    <h2 class="text-2xl font-bold text-navy">Reaktionsschritte</h2>
    <p class="text-base text-gray-600 leading-relaxed">
      Arbeiten Sie die Ransomware-Checkliste Schritt fuer Schritt ab. Alle 25 Punkte muessen bestaetigt werden.
    </p>
    ```

    b) Progress counter (S4-02):
    ```html
    <div class="bg-lightgray rounded-lg p-4 flex items-center justify-between">
      <span class="text-sm font-bold text-navy">{completedCount} von 25 erledigt</span>
      <div class="w-48 h-2 bg-gray-200 rounded-full overflow-hidden">
        <div class="h-full bg-navy rounded-full transition-all" style={{ width: `${percent}%` }} />
      </div>
    </div>
    ```
    where `percent = Math.round((completedCount / totalSteps) * 100)`

    c) Phase groups — map over `RANSOMWARE_PLAYBOOK.phases`, each wrapped in:
    ```html
    <div class="space-y-3">
      <h3 class="text-lg font-bold text-navy">{phase.title}</h3>
      {phase.steps.map(step => ...)}
    </div>
    ```
    Phase groups separated by `space-y-8` (use a parent div with `space-y-8`).

    d) Standard checklist item (S4-04) — for steps WITHOUT noGoWarning:
    ```html
    <label class="flex items-start gap-3 p-4 bg-lightgray rounded-lg cursor-pointer hover:bg-gray-100 transition-colors">
      <input
        type="checkbox"
        checked={completedSteps.includes(step.id)}
        onChange={() => toggleStep(step.id)}
        class="w-5 h-5 accent-navy mt-0.5 shrink-0"
      />
      <div class="flex-1">
        <span class="text-sm text-navy">{step.text}</span>
        <span class="inline-block ml-2 text-xs font-bold bg-navy/10 text-navy px-2 py-0.5 rounded-full">{step.role}</span>
      </div>
    </label>
    ```

    e) No-Go checklist item (S4-05) — for steps WITH noGoWarning:
    ```html
    <div class="space-y-0">
      <label class="flex items-start gap-3 p-4 bg-lightgray rounded-t-lg cursor-pointer">
        <input type="checkbox" checked={...} onChange={...} class="w-5 h-5 accent-navy mt-0.5 shrink-0" />
        <div class="flex-1">
          <span class="text-sm text-navy">{step.text}</span>
          <span class="inline-block ml-2 text-xs font-bold bg-navy/10 text-navy px-2 py-0.5 rounded-full">{step.role}</span>
        </div>
      </label>
      <div class="border-l-4 border-amber bg-amber/10 rounded-br-lg p-4">
        <p class="text-sm font-normal text-navy">
          <span class="mr-2">&#9888;</span>
          {step.noGoWarning}
        </p>
      </div>
    </div>
    ```

    f) StepNavigator at bottom (S4-07):
    ```html
    <StepNavigator
      currentStep={state.currentStep}
      onNext={() => dispatch({ type: 'NEXT_STEP' })}
      onPrev={() => dispatch({ type: 'PREV_STEP' })}
      isNextDisabled={completedCount < totalSteps}
    />
    ```

    **Outer wrapper Step4Reaktion:**
    ```typescript
    export function Step4Reaktion() {
      return (
        <div className="space-y-6">
          <Step4Content />
        </div>
      )
    }
    ```

    The component must be `'use client'` at top.
    Import: `useWizard` from `../WizardContext`, `StepNavigator` from `../StepNavigator`, `RANSOMWARE_PLAYBOOK` from `@/lib/playbook-data`, `ReaktionData` from `@/lib/wizard-types`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/components/wizard/steps/Step4Reaktion.tsx does NOT import StepForm
    - src/components/wizard/steps/Step4Reaktion.tsx contains `import { RANSOMWARE_PLAYBOOK }` from playbook-data
    - src/components/wizard/steps/Step4Reaktion.tsx contains `useWizard()`
    - src/components/wizard/steps/Step4Reaktion.tsx contains `dispatch({ type: 'UPDATE_STEP', stepKey: 'reaktion'`
    - src/components/wizard/steps/Step4Reaktion.tsx contains `isNextDisabled={completedCount < totalSteps}`
    - src/components/wizard/steps/Step4Reaktion.tsx contains `border-l-4 border-amber bg-amber/10`
    - src/components/wizard/steps/Step4Reaktion.tsx contains `bg-navy/10 text-navy px-2 py-0.5 rounded-full` (role badge)
    - src/components/wizard/steps/Step4Reaktion.tsx contains `von 25 erledigt`
    - src/components/wizard/steps/Step4Reaktion.tsx contains `'use client'` on line 1
    - npx tsc --noEmit exits 0
    - npx vitest run exits 0
  </acceptance_criteria>
  <done>
    Screen 4 renders 25 checklist items in 4 phase groups. Each toggle dispatches UPDATE_STEP to wizard state. Progress bar and counter update reactively. No-Go items have amber warning boxes. Role badges visible. Weiter disabled until 25/25. All existing tests still pass.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — zero TypeScript errors
- `npx vitest run --reporter=verbose` — all tests pass
- Manual: navigate to Screen 4 in browser, verify 4 phase groups visible, checkboxes toggle, progress updates, Weiter disabled until 25/25
</verification>

<success_criteria>
- Step4Reaktion.tsx replaces placeholder with full checklist UI
- 25 items in 4 groups, checkboxes persist in wizard state
- No-Go amber warnings visible on relevant items
- Role badges on every item
- Progress counter and bar update on check/uncheck
- Weiter disabled until all 25 checked; Zurueck always enabled
- TypeScript compiles, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-screens-4-5-response-communication/04-02-SUMMARY.md`
</output>
