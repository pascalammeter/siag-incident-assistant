---
phase: 03-screens-0-3-triage
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/components/wizard/steps/Step1Einstieg.tsx
autonomous: true
requirements: [F3, NF1, NF2]

must_haves:
  truths:
    - "User sees prominent Shit-Happens hero button on Screen 1"
    - "User sees Kurzbeschreibung explaining the wizard purpose"
    - "User can click hero button OR alternative link to advance to step 2"
    - "StepNavigator shows only Zurueck on Screen 1 (no Weiter)"
  artifacts:
    - path: "src/components/wizard/steps/Step1Einstieg.tsx"
      provides: "Hero CTA, Kurzbeschreibung, alternative entry"
      contains: "Shit Happens"
      min_lines: 30
  key_links:
    - from: "src/components/wizard/steps/Step1Einstieg.tsx"
      to: "WizardContext dispatch"
      via: "useWizard().dispatch({ type: 'NEXT_STEP' })"
      pattern: "dispatch.*NEXT_STEP"
---

<objective>
Implement Screen 1 (Einstieg) — the hero landing screen with "Shit Happens" CTA, Kurzbeschreibung, and alternative entry link. No form fields (per D-05).

Purpose: Provides the emotional entry point for stressed users — large, clear, single action.
Output: Fully implemented Step1Einstieg.tsx matching UI-SPEC.
</objective>

<execution_context>
@C:\Users\PascalAmmeter\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\PascalAmmeter\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-screens-0-3-triage/03-CONTEXT.md
@.planning/phases/03-screens-0-3-triage/03-UI-SPEC.md
@.planning/phases/03-screens-0-3-triage/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: StepNavigator now supports showNext prop -->
<!-- WizardShell renders StepNavigator with showNext={false} for step 1 -->
<!-- Step1Einstieg must NOT use StepForm (per D-05) — dispatch NEXT_STEP directly -->

From src/components/wizard/WizardContext.tsx:
```typescript
export function useWizard(): { state: WizardState; dispatch: Dispatch<WizardAction> }
// dispatch({ type: 'NEXT_STEP' }) to advance
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Screen 1 — Einstieg hero page</name>
  <files>src/components/wizard/steps/Step1Einstieg.tsx</files>
  <read_first>
    - src/components/wizard/steps/Step1Einstieg.tsx (current placeholder to replace)
    - .planning/phases/03-screens-0-3-triage/03-UI-SPEC.md (Screen 1 component inventory + copywriting contract)
    - .planning/phases/03-screens-0-3-triage/03-CONTEXT.md (D-05, D-06)
    - src/components/wizard/WizardContext.tsx (useWizard hook signature)
  </read_first>
  <action>
    Replace the entire Step1Einstieg component. Remove StepForm import and usage (per D-05). Use `useWizard()` directly for dispatch.

    Complete replacement:
    ```tsx
    'use client'

    import { useWizard } from '../WizardContext'

    export function Step1Einstieg() {
      const { dispatch } = useWizard()

      const handleAdvance = () => {
        dispatch({ type: 'NEXT_STEP' })
      }

      return (
        <div className="space-y-8">
          {/* Hero Section */}
          <div className="text-center space-y-4">
            <h2 className="text-3xl font-bold text-navy">Sicherheitsvorfall?</h2>
            <p className="text-base text-gray-600 text-center max-w-xl mx-auto">
              Wenn ein Sicherheitsvorfall erkannt wird, fuehrt Sie dieses Modul durch die Erstreaktion.
            </p>
          </div>

          {/* Shit-Happens Hero CTA */}
          <div className="text-center">
            <button
              type="button"
              onClick={handleAdvance}
              className="bg-navy text-white text-2xl font-bold px-12 py-6 rounded-xl min-h-[64px] shadow-lg hover:bg-navy-light transition-colors mx-auto block"
            >
              Shit Happens — Los geht's
            </button>
          </div>

          {/* Kurzbeschreibung */}
          <div className="bg-lightgray rounded-lg p-6">
            <p className="text-base text-navy leading-relaxed">
              Dieser Assistent begleitet Sie Schritt fuer Schritt durch die ersten kritischen Stunden nach einem Cyber-Vorfall. Von der Erfassung ueber die Klassifikation bis zur Reaktion und Meldung.
            </p>
          </div>

          {/* Alternative Entry */}
          <p className="text-sm text-gray-500 text-center">
            Oder direkt:{' '}
            <button
              type="button"
              onClick={handleAdvance}
              className="text-navy underline font-bold"
            >
              Vorfall erfassen
            </button>
          </p>
        </div>
      )
    }
    ```

    Key points:
    - NO StepForm wrapper (per D-05)
    - NO einstiegSchema import (removed)
    - Both hero button and "Vorfall erfassen" link call same `handleAdvance` function
    - Hero button: `min-h-[64px]` (stress-tauglich per NF1.4 — larger than standard 44px)
    - All copy verbatim from UI-SPEC Copywriting Contract
    - StepNavigator handled by WizardShell with `showNext={false}` (from Plan 01)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "StepForm" src/components/wizard/steps/Step1Einstieg.tsx returns 0 matches (removed per D-05)
    - grep "einstiegSchema" src/components/wizard/steps/Step1Einstieg.tsx returns 0 matches (no schema import)
    - grep "useWizard" src/components/wizard/steps/Step1Einstieg.tsx returns >= 1 match
    - grep "NEXT_STEP" src/components/wizard/steps/Step1Einstieg.tsx returns >= 1 match
    - grep "Shit Happens" src/components/wizard/steps/Step1Einstieg.tsx returns 1 match
    - grep "Vorfall erfassen" src/components/wizard/steps/Step1Einstieg.tsx returns 1 match
    - grep "Sicherheitsvorfall" src/components/wizard/steps/Step1Einstieg.tsx returns 1 match
    - grep "min-h-\[64px\]" src/components/wizard/steps/Step1Einstieg.tsx returns 1 match
    - grep "bg-lightgray" src/components/wizard/steps/Step1Einstieg.tsx returns 1 match
    - npx tsc --noEmit exits with code 0
  </acceptance_criteria>
  <done>Screen 1 shows hero "Shit Happens" CTA button, Kurzbeschreibung, and alternative "Vorfall erfassen" link. No StepForm. Both buttons dispatch NEXT_STEP. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npx vitest run` passes (no regressions)
- Screen 1 visible at localhost:3000 after confirming No-Go Interstitial
- Hero button and alternative link both advance to step 2
</verification>

<success_criteria>
- "Shit Happens — Los geht's" button visible, large, navy, centered (F3.1)
- Kurzbeschreibung text visible (F3.2)
- "Vorfall erfassen" alternative link visible (F3.3)
- No StepForm wrapper on Screen 1 (D-05)
- StepNavigator shows only "Zurueck" on Screen 1 (D-06, handled by Plan 01)
</success_criteria>

<output>
After completion, create `.planning/phases/03-screens-0-3-triage/03-02-SUMMARY.md`
</output>
