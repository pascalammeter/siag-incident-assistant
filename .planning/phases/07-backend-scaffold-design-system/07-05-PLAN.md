---
phase: 07
plan: 05
title: Fix Warning Issues (WR-01 through WR-05)
status: planning
wave: 1
autonomous: true
gap_closure: true
tasks: 5
requirements_addressed: []
---

# Phase 7 Plan 05: Fix Warning Issues

**Objective:** Fix 5 warning-level issues from code review.

## Tasks

### Task 1: Async Error Handling Wrapper (WR-01)
- Create asyncHandler utility function in src/utils/error.ts
- Wraps async route handlers with try-catch → next() error forwarding
- Apply to /api/incidents GET and POST routes in src/api/index.ts

### Task 2: Protect Routes with API Key Middleware (WR-02)
- Import validateApiKey from src/utils/auth
- Apply middleware to GET/POST /api/incidents routes
- Verify routes now require X-API-Key header

### Task 3: Fix CORS Origin Fallback (WR-03)
- Update src/utils/cors.ts to fail closed in production
- Check process.env.NODE_ENV === 'production'
- Throw error if CORS_ORIGIN not set in production
- Allow localhost fallback only in development

### Task 4: Improve Error Handler Logging (WR-04)
- Update src/middleware/errorHandler.ts
- Add NODE_ENV check to conditionally log stack traces
- Only expose full stack traces in development mode
- Production: log structured error info (type, message, timestamp) only

### Task 5: Fix Seed Data Timestamps (WR-05)
- Update prisma/seed.ts
- Use static ISO timestamp for regulatory fields instead of new Date()
- Ensure test data has realistic but immutable timestamps

## Verification
- ✓ TypeScript strict mode passes
- ✓ All async handlers properly wrapped
- ✓ /api/incidents endpoints protected by middleware
- ✓ CORS properly configured for production
- ✓ Error logging respects NODE_ENV
- ✓ Seed data uses consistent timestamps
