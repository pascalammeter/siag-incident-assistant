---
phase: 07
plan: 04
title: Fix Critical Security Issues (CR-01 & CR-02)
status: planning
wave: 1
autonomous: true
gap_closure: true
tasks: 2
requirements_addressed: []
---

# Phase 7 Plan 04: Fix Critical Security Issues

**Objective:** Fix 2 critical security vulnerabilities found in code review:
1. CR-01: Timing attack in API key validation (src/utils/auth.ts)
2. CR-02: Untyped Express handler parameters (src/api/swagger.ts)

## Tasks

### Task 1: Fix Timing Attack in API Key Validation
- Replace `===` string comparison with `crypto.timingSafeEqual()`
- Update `src/utils/auth.ts` validateApiKey function
- Add crypto import at top of file
- Verify TypeScript compiles with strict mode

### Task 2: Fix Untyped Express Handler Parameters
- Replace `any` types with proper `Request` and `Response` from express
- Update `src/api/swagger.ts` swaggerJson handler signature
- Ensure Express import includes Request and Response types
- Verify TypeScript compiles

## Verification
- ✓ No TypeScript errors: `npx tsc --noEmit`
- ✓ Both critical issues marked as resolved in code
- ✓ All tests still pass: `npm test`
