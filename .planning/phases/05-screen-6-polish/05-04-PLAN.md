---
phase: 05-screen-6-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/wizard/steps/Step1Einstieg.tsx
  - src/components/wizard/steps/Step4Reaktion.tsx
  - src/components/wizard/steps/Step5Kommunikation.tsx
autonomous: true
requirements:
  - NF1.4

must_haves:
  truths:
    - "The hero button on Step 1 does not overflow the 375px viewport horizontally"
    - "The progress bar container on Step 4 stretches to full available width on mobile"
    - "Template card headers on Step 5 wrap correctly on narrow viewports (no overflow)"
    - "Deadline display on Step 5 stacks vertically on mobile (column) and side-by-side on sm+"
  artifacts:
    - path: "src/components/wizard/steps/Step1Einstieg.tsx"
      provides: "Hero button with responsive padding px-6 sm:px-12"
      contains: "px-6 sm:px-12"
    - path: "src/components/wizard/steps/Step4Reaktion.tsx"
      provides: "Progress bar container with w-full max-w-xs instead of fixed w-48"
      contains: "w-full max-w-xs"
    - path: "src/components/wizard/steps/Step5Kommunikation.tsx"
      provides: "flex-wrap on template headers and flex-col sm:flex-row on deadline display"
      contains: "flex-wrap"
  key_links:
    - from: "src/components/wizard/steps/Step1Einstieg.tsx"
      to: "375px viewport"
      via: "px-6 sm:px-12 responsive padding"
      pattern: "px-6 sm:px-12"
    - from: "src/components/wizard/steps/Step4Reaktion.tsx"
      to: "375px viewport"
      via: "w-full max-w-xs removes fixed 192px width"
      pattern: "w-full max-w-xs"
---

<objective>
Fix the 4 identified mobile layout regressions across Steps 1, 4, and 5, then do a targeted sweep of all step components for additional overflow or touch-target issues at 375px.

Purpose: The SIAG advisor review will happen on various devices. The 3 known broken patterns create horizontal overflow or cramped UI on 375px viewports. Fix these before the review.

Output: Targeted CSS class changes in 3 step components. No structural changes, no new files, no new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@src/components/wizard/steps/Step1Einstieg.tsx
@src/components/wizard/steps/Step4Reaktion.tsx
@src/components/wizard/steps/Step5Kommunikation.tsx
@src/components/wizard/StepNavigator.tsx
@src/components/wizard/WizardProgress.tsx
</context>

<interfaces>
<!-- Tailwind v4 responsive prefix syntax (same as Tailwind v3): -->
<!-- sm: prefix = min-width 640px -->
<!-- px-6 sm:px-12 = 24px padding on mobile, 48px on sm+ -->
<!-- flex-col sm:flex-row = column stack on mobile, row on sm+ -->
<!-- w-full max-w-xs = full width but capped at 320px (avoids fixed 192px = w-48) -->

<!-- Touch target minimum: min-h-[44px] — already used in StepNavigator buttons -->
<!-- Verified: StepNavigator already has min-h-[44px] on both buttons — no change needed there -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix Step1, Step4, Step5 mobile CSS issues</name>
  <files>
    src/components/wizard/steps/Step1Einstieg.tsx
    src/components/wizard/steps/Step4Reaktion.tsx
    src/components/wizard/steps/Step5Kommunikation.tsx
  </files>
  <action>
Make these TARGETED CLASS CHANGES only. Read each file first, locate the exact element, change only the listed class. Do not restructure JSX or change logic.

---

**Fix 1 — Step1Einstieg.tsx: Hero button overflow on 375px**

Find the main "Shit Happens" / primary CTA button. It will have `px-12` in its className.

Change `px-12` to `px-6 sm:px-12`.

If there are multiple buttons with `px-12`, apply this change to each one. The fix prevents horizontal scroll on 375px while keeping the desktop appearance unchanged.

---

**Fix 2 — Step4Reaktion.tsx: Progress bar container fixed width**

Find the progress bar container div. Based on the file content, it is:
```tsx
<div className="w-48 h-2 bg-gray-200 rounded-full overflow-hidden">
```

Change `w-48` to `w-full max-w-xs`:
```tsx
<div className="w-full max-w-xs h-2 bg-gray-200 rounded-full overflow-hidden">
```

This allows the bar to scale to full available width on mobile while staying capped at 320px on larger screens.

---

**Fix 3 — Step5Kommunikation.tsx: Template card header overflow**

Find the template card header sections. Each template card (GL, Mitarbeitende, Medien) has a header row containing the title and possibly a "Kopieren" button. The header uses `flex` without a wrap — look for a pattern like:
```tsx
<div className="flex items-center justify-between ...">
  <h4 ...>Vorlage: ...</h4>
  <button ...>Kopieren</button>
</div>
```
or similar flex container on the card header.

Add `flex-wrap gap-2` to that flex container:
```tsx
<div className="flex flex-wrap gap-2 items-center justify-between ...">
```

If the template header already has `flex-wrap`, skip this fix and note it in the summary.

---

**Fix 4 — Step5Kommunikation.tsx: Deadline display stacking**

Find the deadline display section in MeldepflichtSection or the deadline rendering area. It will have a `flex` container showing deadline label + date side by side. Look for a pattern like:
```tsx
<div className="flex items-center gap-2 ...">
  <span>Frist:</span>
  <span>{deadline}</span>
</div>
```
or a `flex justify-between` container.

Change to `flex flex-col sm:flex-row gap-2`:
```tsx
<div className="flex flex-col sm:flex-row gap-2 items-start sm:items-center ...">
```

If there are multiple deadline display rows, apply to each.

---

**Sweep (do after all 4 fixes above):**

Read these files (if not already loaded) and scan for:
1. Any `min-w-[N]` or `w-[Npx]` fixed widths that could overflow 375px — change to `w-full max-w-[N]` or remove
2. Any buttons missing `min-h-[44px]` — add it
3. Any `flex` without `flex-wrap` where content could overflow — add `flex-wrap gap-2`

Files to sweep:
- `src/components/wizard/steps/Step2Erfassen.tsx`
- `src/components/wizard/steps/Step3Klassifikation.tsx`
- `src/components/wizard/StepNavigator.tsx` (already confirmed min-h-[44px] — verify)
- `src/components/wizard/WizardProgress.tsx` (check label row overflow)

Apply only fixes that are clearly necessary. Do NOT make speculative changes. If unsure whether something overflows, leave it and note in the summary.
  </action>
  <verify>
    <automated>cd /c/Users/PascalAmmeter/ClaudeCode/siag-incident-assistant && grep -n "px-6 sm:px-12" src/components/wizard/steps/Step1Einstieg.tsx && grep -n "w-full max-w-xs" src/components/wizard/steps/Step4Reaktion.tsx && grep -n "flex-wrap" src/components/wizard/steps/Step5Kommunikation.tsx && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - `Step1Einstieg.tsx`: hero button has `px-6 sm:px-12` (no bare `px-12`)
    - `Step4Reaktion.tsx`: progress bar container has `w-full max-w-xs` (no bare `w-48`)
    - `Step5Kommunikation.tsx`: template card header(s) have `flex-wrap`; deadline display has `flex-col sm:flex-row`
    - TypeScript compiles without errors
    - Sweep completed — any additional fixes documented in SUMMARY.md
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero errors.
2. Browser DevTools: Set viewport to 375px width. Navigate through all steps:
   - Step 1: Hero button fully visible, no horizontal scroll
   - Step 4: Progress bar stretches to available width, no fixed 192px cap
   - Step 5: Template card headers wrap if title + button are too wide; deadline stacks vertically
3. Check all buttons are ≥ 44px tall (DevTools computed height). StepNavigator buttons already have `min-h-[44px]`.
</verification>

<success_criteria>
- Step 1 hero button: `px-6 sm:px-12` — no horizontal overflow on 375px (NF1.4)
- Step 4 progress bar: `w-full max-w-xs` — scales to full width on mobile (NF1.4)
- Step 5 template headers: `flex-wrap` — no overflow on 375px (NF1.4)
- Step 5 deadline: `flex-col sm:flex-row` — stacks on mobile (NF1.4)
- Sweep complete — any additional issues found either fixed or documented
- TypeScript compiles cleanly
- No structural JSX changes, no logic changes, no new dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/05-screen-6-polish/05-04-SUMMARY.md`

Include in the summary:
- Which of the 4 known fixes were applied (and whether any were already correct)
- Any additional issues found and fixed during the sweep
- Any issues found but NOT fixed (with reason)
</output>
