---
phase: 05-screen-6-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - public/siag-logo.svg
  - src/app/layout.tsx
  - src/components/Header.tsx
autonomous: true
requirements:
  - NF2.1
  - NF2.3
  - NF2.5
  - NF5.3

must_haves:
  truths:
    - "The header shows an SVG logo image instead of the plain 'SIAG' text div"
    - "Inter font is loaded via next/font/google and applied to the body element"
    - "The SVG logo file exists at public/siag-logo.svg"
  artifacts:
    - path: "public/siag-logo.svg"
      provides: "SIAG logo placeholder SVG with navy fill and white text"
      contains: "SIAG"
    - path: "src/app/layout.tsx"
      provides: "Inter font loaded via next/font/google and applied to html element"
      contains: "next/font/google"
    - path: "src/components/Header.tsx"
      provides: "Header using <img> tag referencing /siag-logo.svg"
      contains: "siag-logo.svg"
  key_links:
    - from: "src/app/layout.tsx"
      to: "Inter font (Google Fonts CDN)"
      via: "next/font/google Inter import"
      pattern: "next/font/google"
    - from: "src/components/Header.tsx"
      to: "public/siag-logo.svg"
      via: "<img src=\"/siag-logo.svg\" />"
      pattern: "siag-logo.svg"
---

<objective>
Polish SIAG branding: create an SVG logo placeholder, load Inter font properly via `next/font/google`, and update the Header to display the logo image.

Purpose: The project currently references Inter in CSS but never loads it (no font source). The header has a plain text div instead of a logo image. Both are must-haves for the SIAG advisor review (NF2.5 logo, NF2.3 typography).

Output:
- `public/siag-logo.svg` — placeholder SVG clearly marked as such
- `src/app/layout.tsx` — Inter loaded via `next/font/google`, applied to `<html>`
- `src/components/Header.tsx` — logo image replacing the text div
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@src/app/layout.tsx
@src/components/Header.tsx
@src/app/globals.css
</context>

<interfaces>
<!-- Current src/app/layout.tsx: -->
```typescript
import type { Metadata } from "next";
import "./globals.css";
import { Header } from "@/components/Header";
import { Footer } from "@/components/Footer";

export const metadata: Metadata = {
  title: "SIAG Incident Management Assistent",
  description: "Incident-Response-Wizard fuer SIAG-Berater",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="de">
      <body className="min-h-screen flex flex-col bg-white text-navy">
        <Header />
        <main className="flex-1 max-w-4xl mx-auto w-full px-6 py-8">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
```

<!-- Current src/components/Header.tsx: -->
```typescript
export function Header() {
  return (
    <header className="bg-navy text-white px-6 py-4">
      <div className="max-w-4xl mx-auto flex items-center gap-4">
        <div className="w-10 h-10 bg-white/20 rounded flex items-center justify-center text-sm font-bold">
          SIAG
        </div>
        <h1 className="text-lg font-semibold">Incident Management Assistent</h1>
      </div>
    </header>
  );
}
```

<!-- next/font/google usage pattern for Next.js App Router: -->
```typescript
import { Inter } from 'next/font/google'
const inter = Inter({ subsets: ['latin'] })
// Apply: <html lang="de" className={inter.className}>
// NOTE: apply to <html>, not <body> — the className goes on the html element
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create public/siag-logo.svg</name>
  <files>public/siag-logo.svg</files>
  <action>
Create a rectangular SVG placeholder logo. Requirements:
- Dimensions: `width="120" height="40"` viewBox="0 0 120 40"`
- Background rectangle: navy fill `#1a2e4a`, full size, `rx="4"` (slightly rounded)
- "SIAG" text: white fill, `font-size="16"`, `font-family="Inter, system-ui, sans-serif"`, `font-weight="700"`, centered at `x="60" y="26"` with `text-anchor="middle"`
- A small indicator that this is a placeholder: a tiny "(placeholder)" text in light gray `#9ca3af`, `font-size="7"` positioned at `x="60" y="37"` with `text-anchor="middle"` — subtle, not distracting

Full SVG content:
```xml
<svg xmlns="http://www.w3.org/2000/svg" width="120" height="40" viewBox="0 0 120 40">
  <rect width="120" height="40" rx="4" fill="#1a2e4a"/>
  <text x="60" y="26" font-family="Inter, system-ui, sans-serif" font-size="16" font-weight="700" fill="#ffffff" text-anchor="middle">SIAG</text>
  <text x="60" y="37" font-family="Inter, system-ui, sans-serif" font-size="7" fill="#9ca3af" text-anchor="middle">(placeholder)</text>
</svg>
```
  </action>
  <verify>
    <automated>test -f /c/Users/PascalAmmeter/ClaudeCode/siag-incident-assistant/public/siag-logo.svg && grep -c "SIAG" /c/Users/PascalAmmeter/ClaudeCode/siag-incident-assistant/public/siag-logo.svg</automated>
  </verify>
  <done>
    - File exists at `public/siag-logo.svg`
    - Contains valid SVG with navy background rectangle and "SIAG" text
    - Contains "(placeholder)" sub-label
  </done>
</task>

<task type="auto">
  <name>Task 2: Load Inter via next/font/google and update Header to use logo image</name>
  <files>
    src/app/layout.tsx
    src/components/Header.tsx
  </files>
  <action>
**src/app/layout.tsx — add Inter font and apply to html element:**

Replace the entire file with this content (preserving all existing imports and metadata):

```tsx
import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Header } from "@/components/Header";
import { Footer } from "@/components/Footer";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "SIAG Incident Management Assistent",
  description: "Incident-Response-Wizard fuer SIAG-Berater",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="de" className={inter.className}>
      <body className="min-h-screen flex flex-col bg-white text-navy">
        <Header />
        <main className="flex-1 max-w-4xl mx-auto w-full px-6 py-8">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
```

Key changes from original:
- Added `import { Inter } from "next/font/google"`
- Added `const inter = Inter({ subsets: ["latin"] })`
- Added `className={inter.className}` to `<html>` tag (NOT body — font/google requires it on the html element to avoid hydration issues)

**src/components/Header.tsx — replace the SIAG text div with an img tag:**

Replace the entire file with this content:

```tsx
export function Header() {
  return (
    <header className="bg-navy text-white px-6 py-4">
      <div className="max-w-4xl mx-auto flex items-center gap-4">
        <img
          src="/siag-logo.svg"
          alt="SIAG"
          className="h-8 w-auto"
        />
        <h1 className="text-lg font-semibold">Incident Management Assistent</h1>
      </div>
    </header>
  );
}
```

Note: `src/components/Header.tsx` has no `'use client'` directive and none is needed — it is a Server Component. Do NOT add `'use client'`. The `<img>` tag with a static `/` path works correctly in Next.js static export (`output: 'export'`).
  </action>
  <verify>
    <automated>cd /c/Users/PascalAmmeter/ClaudeCode/siag-incident-assistant && npx tsc --noEmit 2>&1 | head -20 && grep -n "next/font/google\|inter.className" src/app/layout.tsx && grep -n "siag-logo.svg" src/components/Header.tsx</automated>
  </verify>
  <done>
    - `src/app/layout.tsx` imports Inter from `next/font/google` and applies `inter.className` to `<html>`
    - `src/components/Header.tsx` uses `<img src="/siag-logo.svg" alt="SIAG" className="h-8 w-auto" />`
    - TypeScript compiles without errors
    - `globals.css` still references Inter in `--font-sans` — this is fine and complementary (font/google loads the actual font file; globals.css declares the font-family fallback stack)
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero errors.
2. `npm run build` — static export builds without warnings related to font or layout.
3. Open browser: header shows the SVG logo image (navy rectangle with "SIAG" text) instead of the plain text box. Font renders as Inter (verify via browser DevTools: computed font-family on body should show "Inter").
</verification>

<success_criteria>
- `public/siag-logo.svg` exists and contains a valid SVG logo placeholder (NF2.5)
- Header renders `<img src="/siag-logo.svg" />` instead of the text div
- Inter font loads from Google Fonts via `next/font/google` (NF2.3)
- `inter.className` applied to `<html>` element in layout.tsx
- TypeScript compiles cleanly, build succeeds
- No new npm packages added beyond what next/font/google provides natively
</success_criteria>

<output>
After completion, create `.planning/phases/05-screen-6-polish/05-03-SUMMARY.md`
</output>
